{
  "info": {
    "name": "UnclogMe - GDO Online Reporting Bot API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "description": "> _This description is generated from `postman/README.md` (README last updated 2026-09-18, second change) by `scripts/postman/sync_collection_description.js`. Edit the README, run the script, re-import the collection. A stale copy fails the pre-commit hook and the postman-doc-drift workflow._\n\n# GDO Online Reporting API\n\n**Audience:** the integrator of the GDO Online Reporting RPA bot (Jonathan / \"John\").\n**Status:** LIVE, filing real reports to Miami-Dade. 7 confirmed filings since 2026-07-24.\n**Last updated:** 2026-09-18, second change (🛑 **`gallons` is no longer always null: on a ticket offloaded OUTSIDE Miami-Dade every row carries the client's grease trap CAPACITY, with `gallons_source` and a ticket-level `dade_pickup_gallons` block; white tickets stay null. Yan's decision, see §4c**). Earlier the same day (the dry-run queue now serves a mixed sample across jurisdictions so a Broward yellow ticket is always in it, and the served-but-undocumented `ticket_number` / `jurisdiction` fields are in the report-object table, see §3). Previously 2026-08-31 (🛑 **the queue now serves EVERY permit on a ticket in one batch, so `visit_id` is no longer unique within a batch and the work key is the pair `(visit_id, gdo_id)`; the result POST is idempotent on `(visit_id, gdo_id, run_id)` and `gdo_id` is now USED rather than ignored**, see §3 and §4). Previously 2026-08-26 (added `truck_decal`, the `?unreported=1` mode and the mark-as-reported endpoint §4d; corrected the quantity/fee claim in §4c; documented that `truck_decals` is manifest-grained, not filing-grained, and that the address fields are null together on the no-property case, in §4c. **Post-ship audit the same day corrected four things: the `dry_run` example defaulted to a REAL filing, the append-only claim in §4d rule 2 was not implemented at the grant level until `2026-08-26_1815`, the wrong-key test was sending the valid key, and the row-cap guard was unreachable.**)\n\n> This is both the **API reference** and the doc for the Postman collection in this folder. It\n> documents the **current** contract of the two endpoints your bot talks to, plus the surrounding\n> pieces (event push, status surfaces, evidence storage, error codes). Nothing here is a secret: key\n> **values** are sent to you privately, never committed. This repo is public.\n\n---\n\n## 1. What this is\n\nThe bot files **GDO Online Reporting** on the Miami-Dade DERM portal. For us that is **Line Item 27**\non a client's job, so the **unit of work is a serviced VISIT** for one of the clients that carry that\nadd-on, not a manifest — but the queue is **deduped per dump**, so you never receive the same manifest\ntwice (see §3). When we service such a client and their DERM manifest is filed + linked to the\nvisit, that visit becomes a \"please file the online report\" job for the bot.\n\nIt is a **small, targeted feed** (only a few clients carry Line Item 27), **server-to-server**, and\n**compliance-critical**: the design's first job is to *never double-file a report to the county*. All\nthe lifecycle rules below exist to guarantee that.\n\nTwo endpoints, both live:\n\n| Direction | Method | Endpoint | Purpose |\n|---|---|---|---|\n| bot → us | `GET`  | `/functions/v1/rpa-derm-queue`  | \"What should I report?\" |\n| bot → us | `POST` | `/functions/v1/rpa-derm-result` | \"Here is what happened.\" |\n\nBase URL: `https://wbasvhvvismukaqdnouk.supabase.co`\n\n---\n\n## 2. Authentication\n\nEvery call carries the header **`x-rpa-key`** (value sent to you privately). There is **no Supabase\nJWT** and no database connection: the key is the auth, checked inside each function.\n\n- There is **one key** — the `x-rpa-key` you send us on every `GET`/`POST` above. It is matched\n  against a **comma-separated** secret on our side, so we can rotate with zero downtime (add new,\n  you switch, we remove old). You never see the list.\n- The key lives only in your Railway environment variables, never in code or logs.\n\nMissing/invalid key → `401 {\"error\":\"unauthorized\"}`. Key not configured on our side →\n`503 {\"error\":\"service_not_configured\"}`.\n\n---\n\n## 3. `GET /functions/v1/rpa-derm-queue` — the work queue\n\nReturns up to **25** reports to file, **oldest first** (live mode; the dry-run sample is ordered\ndifferently, see the `mode` table below).\n\n### 🛑 One report per ACTIVE PERMIT, not per ticket (changed 2026-08-11)\n\nWe pump **both** grease traps on a visit, so a client holding N active GDO permits owes Miami-Dade\n**N reports per qualifying dump ticket**. Until 2026-08-11 this queue offered one, which was\nunder-reporting.\n\n| | |\n|---|---|\n| `gdo_number` | now means **the permit this row is for**, not \"the client's permit\" |\n| `gdo_id` | **new column**, our stable integer id for that permit |\n\n🛑 **SUPERSEDED 2026-08-31. THE TWO PARAGRAPHS BELOW WERE TRUE UNTIL THEN AND ARE NOW WRONG.** They\nare kept because you built against them, so you need to see exactly what changed:\n\n> ~~**You still receive at most one permit per ticket at a time.** The 20-hour dispense lease is held\n> against the whole ticket, so the remaining permits stay back until the served one is filed. A\n> multi-permit ticket therefore reappears on a later poll carrying its next permit, and a three-permit\n> client (009-CN Casa Neos) files over roughly two days.~~\n>\n> ~~**Nothing about your contract changes.** Batch cap is still 25 rows, `visit_id` is still unique\n> within a batch, and you do **not** send the permit back: we resolve it ourselves from the pair we\n> served.~~\n\n### 🛑 You now receive EVERY permit on a ticket, in the SAME batch (changed 2026-08-31)\n\nThe 20-hour lease is now held per **(visit, permit)** instead of per ticket, and the queue is keyed on\n`(manifest_id, gdo_id)`. A three-permit ticket comes back as **three rows in one pull**, and you may\nfile all three in a single run.\n\n**Two things this changes for you:**\n\n1. 🛑 **`visit_id` is NO LONGER UNIQUE WITHIN A BATCH.** Casa Neos returns three rows that all carry\n   `visit_id: 6568` and differ only by `gdo_id`. **If your bot de-duplicates work by `visit_id`, it\n   will silently drop two of the three filings.** The work key is now the **pair**\n   `(visit_id, gdo_id)`.\n2. **Please echo `gdo_id` back on the result POST** (section 4). It is optional and nothing breaks\n   without it, but see the note there: without it we have to infer which permit a result was for, and\n   the inference is only reliable when every filing in the run succeeds.\n\nEverything else is unchanged: batch cap is still 25 rows, the same `x-rpa-key`, the same fields.\n\n⚠ **Why it was one-at-a-time before, so you know we did not simply forget:** serving one permit per\nticket made the existing ticket-wide lease into a free \"only one in flight\" guarantee, and the DERM\ndeadline is the 15th of the following month, so a three-permit client filing over two days cost\nnothing. It stopped being acceptable when it read as a *missed* filing rather than a slow one.\n\n⚠ Permit numbers render three different ways. The county email prints the bare integer (`12517`), our\n`gdo_number` is a zero-padded string (`GDO-09853`), and the permit PDF prints `GDO-012517-2026/2026`.\n**Compare as integers everywhere.** Four integer collisions exist across 135 active permits, all of\nthem the same number held by two different client records, and **none within a single client**, so\ninteger matching can never merge one client's own permits.\n\n**Query params**\n\n| Param | Values | Meaning |\n|---|---|---|\n| `mode` | `dryrun` | Serve a **sample of historical** visits for testing, no lease. Omit for the live queue. |\n\n🛑 **The dry-run sample covers EVERY jurisdiction (changed 2026-09-18).** Until then dry-run served\nthe 25 **oldest** historical rows, which were all Miami-Dade (January to April 2026); the Broward\nrows in the dry-run set sat past the cap, so a bot tested only against dry-run had never once seen a\n**yellow ticket** (`jurisdiction: \"broward\"`, `disposal_facility: \"Water and Wastewater Services\"`,\n`white_manifest_number: null`, the number to file in `ticket_number`). The sample is now **newest\nfirst within each jurisdiction, interleaved across jurisdictions** (broward, dade, broward, dade,\ndade ...), still capped at 25. It is a **sample, not a queue**: do not build on its order, and\nexpect the same rows on every pull until a manifest changes. Live mode is untouched: oldest first,\ncapped, leased.\n\n**Response `200`**\n\n```jsonc\n{\n  \"generated_at\": \"2026-07-21T15:00:00.000Z\",\n  \"mode\": \"live\",              // or \"dryrun\"\n  \"count\": 3,\n  \"reports\": [ { /* report object, see below */ } ]\n}\n```\n\n**Report object** — every field the bot needs to file one report:\n\n| Field | Type | Notes |\n|---|---|---|\n| `visit_id` | integer | Echo it back on your result POST. ⚠ **No longer unique within a batch** since 2026-08-31: see section 3's permit note. |\n| `gdo_id` | integer | **Added 2026-08-31. Together with `visit_id` this is the work key.** Our stable id for the permit this row is for. **Echo it back on your result POST.** |\n| `manifest_id` | integer | The linked DERM manifest (context; optional to echo). |\n| `dry_run` | boolean | `true` when this came from `?mode=dryrun`. |\n| `client_code` | string | e.g. `041-MB`. |\n| `client_name` | string | |\n| `client_email` | string \\| null | |\n| `address`, `city`, `zip`, `county` | string | Service location. |\n| `gdo_number` | string \\| null | The GDO permit number. |\n| `service_date` | date | When the visit was serviced. |\n| `dump_ticket_date` | date | Date on the disposal receipt. |\n| `ticket_number` | string | **The dump-ticket number to file, whatever the county** (served since 2026-07-24, documented here 2026-09-18): the Miami-Dade white manifest # or the Broward yellow Septage Receiving #. Use this, not `white_manifest_number`. |\n| `jurisdiction` | string | `dade` \\| `broward` \\| `unknown`, from which ticket number the manifest carries (served since 2026-07-24, documented here 2026-09-18). |\n| `white_manifest_number` | string \\| null | Miami-Dade WWTP/dump receipt number. **`null` on a Broward load**; kept for back-compat, read `ticket_number`. |\n| `disposal_facility` | string \\| null | `South District WWTP` (Miami-Dade) or `Water and Wastewater Services` (Broward). |\n| `documents.address` | string[] | **Signed URLs** to the DERM Address / Manifest Form sheet(s). |\n| `documents.receipt` | string[] | **Signed URLs** to the Transporter Manifest / WWTP receipt(s). |\n\n**Signed URLs are valid ~4 hours** and are minted fresh on every fetch. Download everything at the\nstart of a run; **never log or persist the URLs**.\n\n### Queue lifecycle (why a visit does or does not appear)\n\n- **One row per (dump, PERMIT).** 🛑 **Changed 2026-08-31; this used to say one row per dump.** A\n  county report is owed per **permit** on a dump ticket, so a dump with three active permits returns\n  **three rows**, identical except for `gdo_id` / `gdo_number`. Where more than one visit fed the dump\n  it is still represented by a single visit (the one whose service date is closest to the dump), so you\n  never see the same **(dump, permit)** pair twice in a batch. **The work key is the pair\n  `(visit_id, gdo_id)`, not `visit_id` alone.**\n- A dump stays in the live queue **until it is successfully reported** — so nothing is dropped if the\n  bot is down for a while.\n- It leaves the queue **permanently** once the county confirms it (a `SUCCESS` result with a\n  `portal_confirmation`, section 4). This is **manifest-scoped**: reporting *any* visit of a dump clears\n  the whole dump, so a sibling visit can never re-surface an already-filed report.\n- After any attempt — or the moment we hand it to you — the dump is **held for 20 hours** (a dispense\n  *lease*, also manifest-scoped) so a crash between filing and the result POST can never cause a\n  re-dispense and a double-file.\n- The live queue only contains visits with a `dump_ticket_date` on/after a **launch cutoff** (currently\n  `2026-07-21`). This exists so the bot's first live run cannot re-submit the entire historical backlog\n  to the county. We widen the cutoff deliberately if a backfill is ever wanted.\n- `?mode=dryrun` serves already-serviced **historical** visits and does **not** place a lease; use it\n  freely for testing. Since 2026-09-18 it is a mixed sample across jurisdictions (see the `mode`\n  table in section 3), so a Broward yellow ticket is always in it.\n\n### 🛑 `held` — why the queue is empty (added 2026-08-25)\n\n**Asked for by Jonathan:** *\"the queue read empty on the 21st–23rd while 11024 sat excluded, so\n'empty' and 'stuck' looked identical.\"* He was right. `count: 0` used to mean **both** \"nothing to\nfile\" and \"a filing is stuck and nobody can see it\". Every response now carries a `held` block:\n\n```jsonc\n\"held\": {\n  \"available\": true,\n  \"total\": 8,                       // REAL holds\n  \"by_reason\": { \"already_filed\": 8 },\n  \"not_held\": {                     // NOT problems, listed so they are not mistaken for holds\n    \"sibling_won_this_pass\": 0,\n    \"before_launch_cutoff\": 51\n  }\n}\n```\n\n**The reasons, and what each means for you**\n\n| reason | what it means | needs you? |\n|---|---|---|\n| `already_filed` | the county confirmed it. The healthy steady state | no |\n| `cooldown_20h` | an attempt landed in the last 20h | no, it serves after |\n| `data_error` | a **non-retryable** failure holds it until something about the row **changes** | **maybe — see below** |\n| `leased` | we handed the manifest out and are holding it 20h | no |\n\n⚠ **`data_error` is the one to watch, and it is the one that bit us.** Its freshness anchor is a\ntimestamp **in our database**. If you fix the cause on the **county side** — a portal credential, a\npermit registration — that fix is structurally invisible to the gate and the row will never come\nback on its own, no matter how long you poll. **Tell us and we re-open it in one call.**\n\n⚠ **`not_held` entries are not problems.** `sibling_won_this_pass` means the queue serves one permit\nper manifest per pass and another permit won this one; it comes up next pass.\n`before_launch_cutoff` is the historical backlog we deliberately do not file.\n\n⚠ **`available: false` means the summary itself could not be computed** — the queue is still valid,\nbut do not read a missing `held` as \"nothing held\". An absent summary and an empty one are different\nanswers, which is the whole point of this block.\n\n**`count` and `reports` are unchanged.** This is purely additive.\n\n---\n\n## 4. `POST /functions/v1/rpa-derm-result` — record an outcome\n\nSame `x-rpa-key`. **Idempotent on `(visit_id, gdo_id, run_id)`**: a retried POST returns\n`200 {deduped:true}` and changes nothing (first write wins).\n🛑 **Changed 2026-08-31; the key used to be `(visit_id, run_id)`.** It had to widen, because a\nticket's permits can now all be filed in one run and the old key would have **rejected your 2nd and\n3rd results** as duplicates of the first. Nothing you do changes: a fresh `run_id` per attempt still\nworks exactly as before, and a genuine retry still dedupes.\n\n**Request body**\n\n| Field | Type | Req | Notes |\n|---|---|---|---|\n| `visit_id` | integer | ✅ | From the queue. |\n| `manifest_id` | integer | | Optional; echo it back if you have it. |\n| `run_id` | string | ✅ | Charset `[A-Za-z0-9_.-]`, ≤100 chars. **Generate ONCE per report attempt, before you hit the portal, and reuse it verbatim on every retry.** It becomes part of the screenshot storage key. |\n| `status` | string | ✅ | Short uppercase code, `^[A-Z0-9_]{1,64}$`. Use the literal **`SUCCESS`** for a confirmed report (see below). |\n| `retryable` | boolean | ✅ | `true` for transient problems (portal timeout); `false` for data problems (missing field). |\n| `failure_reason` | string | | ≤1000 chars. |\n| `attempted_at` | string | ✅ | ISO 8601 UTC. **Log/audit only** — our queue timing uses our own server clock, so a skewed bot clock can never cause a double-file. |\n| `portal_confirmation` | string | | ≤200 chars. Whatever number/text the portal returns. **Required for a real `SUCCESS`.** |\n| `screenshot` | string | | base64 **JPEG or PNG**, ≤5 MB decoded. We detect the real format from the bytes and store the matching extension and content-type, so send whatever you have and do not re-encode. |\n| `screenshot_missing_reason` | string | | ≤300 chars. **Provide this OR `screenshot`** — every result needs one. |\n| `dry_run` | boolean | | Ignored / derived server-side; harmless to send. |\n\nUnknown fields → `400 {\"error\":\"unknown_field_<name>\"}`.\n\n**Two hard rules enforced on our side:**\n\n1. **No optimistic success.** A visit only counts as reported (permanent queue exit) when\n   `status = 'SUCCESS'` **and** a `portal_confirmation` is present. Any other confirmed status is fine\n   too as long as you send `portal_confirmation`, but `SUCCESS` is the clean permanent-done signal.\n2. **Evidence or a reason.** Every result carries a `screenshot` **or** an explicit\n   `screenshot_missing_reason`. If a screenshot is oversized/undecodable we still **record the result**\n   and just flag the missing evidence — we never drop a genuine attempt over a screenshot problem.\n\n**Responses**\n\n| Code | Body | Meaning |\n|---|---|---|\n| `201` | `{\"recorded\":true,\"deduped\":false,\"id\":<n>,\"screenshot_stored\":<bool>,\"screenshot_missing_reason\":<str\\|null>}` | New result stored. |\n| `200` | `{\"recorded\":true,\"deduped\":true,\"id\":<n>,\"screenshot_stored\":<bool>,\"screenshot_missing_reason\":<str\\|null>}` | Duplicate `(visit_id, run_id)`; no-op ack. |\n\n**`screenshot_stored` (added 2026-08-11)** tells you whether the image is actually in the bucket.\n\nAn oversize, undecodable or unstorable image is **still recorded as a filing and still answers 201**,\nwith the image dropped. That is deliberate: a real county filing must never be lost over a screenshot\nproblem. Before this field existed you could not tell \"filed with evidence\" from \"filed without it\"\nby reading the response. Now you can.\n\n- `screenshot_missing_reason` carries **our** drop reason when we dropped it: `SCREENSHOT_TOO_LARGE`,\n  `SCREENSHOT_DECODE_FAILED`, `STORE_FAILED`. It overrides whatever you sent in that field.\n- On a **deduped 200** both fields describe the **stored row**, not what you just posted, because a\n  dedupe uploads nothing. That is the number that tells you whether the stored filing has evidence.\n\n🛑 **`gdo_id` IS NOW USED (changed 2026-08-31). Please send it.** `gdo_number` is still accepted\nand ignored.\n\nSend back the `gdo_id` from the queue row you just filed. We validate it against that visit's real\npermit set before trusting it, so a wrong value is refused rather than mis-recorded, and a filing is\nnever rejected over this field.\n\n**It is optional and nothing breaks without it**, but here is the honest reason to send it. When you\ndo not, we infer the permit server-side as *the lowest still-unfiled permit for that visit*. That was\nexact while the queue served one permit at a time. Now that a run can carry all of a ticket's permits,\nthe inference attributes **by arrival order**: if you file permits A and C and A fails, the failure is\nrecorded against A and C's success is then also recorded against A. Sending `gdo_id` makes the\nattribution certain instead of inferred.\n\n⚠ This is why both fields were accepted-and-ignored from 2026-08-11: unknown fields are a `400`, and a\nrejected result is a county filing we have no record of. That reasoning is unchanged; only the\nusefulness of `gdo_id` is.\n\n**If you do not get a 2xx, keep the result locally and retry the same POST** (same `run_id`) until you\ndo. Never treat a report as done before we acknowledge it.\n\n**Screenshots:** send the bytes in the POST; we store them in a **private** bucket (`rpa-evidence`,\nclient data) and record only the path. You never need storage credentials. If images ever exceed the\n5 MB cap routinely we will switch you to a signed upload URL.\n\n---\n\n## 4b. `POST /functions/v1/rpa-derm-evidence`: attach the image LATER\n\n**Added 2026-08-24, for the case where the filing succeeds but its evidence is not ready yet.**\nThe county confirmation email usually arrives the same minute as the submit, but not always. Rather\nthan hold the run open or post a result you cannot evidence, **post the result first and attach the\nimage when it arrives.**\n\nSame `x-rpa-key`. Body:\n\n```jsonc\n{\n  \"visit_id\": 6216,                    // required\n  \"run_id\": \"5133d0d7-...\",            // required, the SAME run_id you used on the result\n  \"screenshot\": \"<base64 JPEG or PNG>\" // required\n}\n```\n\n`manifest_id` and `dry_run` are accepted and ignored, so you can reuse your result body verbatim.\nAny other field is a `400`.\n\n**Responses**\n\n| Code | Body | Meaning |\n|---|---|---|\n| `200` | `{\"attached\":true,\"id\":<n>,\"screenshot_path\":\"...\",\"screenshot_stored\":true,\"cleared_missing_reason\":\"<what it said before>\"}` | Stored. Any `screenshot_missing_reason` on the row was cleared in the same write. |\n| `200` | `{\"attached\":false,\"already_had_evidence\":true,\"id\":<n>,\"screenshot_path\":\"...\"}` | That result already has an image. **Not an error**. This is the expected answer to a retry. |\n| `404` | `{\"error\":\"result_not_found_for_visit_run\"}` | No result stored for that `(visit_id, run_id)`. |\n\n🛑 **FILL-ONCE. This endpoint can only ever turn \"no image\" into \"an image\".** It will never replace\nan image we already hold, not even with a different one. That means **you can retry it as often as\nyou like, in any order, and concurrently with yourself, and it cannot do damage.** If you genuinely\nneed an image corrected, that is a human decision and staff do it in the DERM Tracker.\n\n🛑 **POST THE RESULT FIRST.** Evidence for a `(visit_id, run_id)` we have never seen is a `404`, not\na stored orphan. The intended sequence is always:\n\n1. `POST /rpa-derm-result` with your `screenshot_missing_reason` (say why it is not there yet), then\n2. `POST /rpa-derm-evidence` with the same `run_id` once you have the image.\n\n**Errors**\n\n| Status | Code | Cause |\n|---|---|---|\n| 400 | `invalid_json` | Body was not JSON. |\n| 400 | `unknown_field_<name>` | A field we do not accept. |\n| 400 | `visit_id_required_integer` | Missing/invalid `visit_id`. |\n| 400 | `run_id_must_be_alnum_dot_dash_underscore_max100` | Bad `run_id` charset/length. |\n| 400 | `screenshot_required` | No image in the body. |\n| 400 | `screenshot_too_large` | Over 5 MB decoded. |\n| 400 | `screenshot_decode_failed` | Not valid base64. |\n| 400 | `screenshot_must_be_jpeg_or_png` | The bytes are not a JPEG or a PNG. |\n| 404 | `result_not_found_for_visit_run` | Post the result first. |\n| 409 | `multiple_results_for_visit_run` | Should be impossible; tell us if you see it. |\n| 500 | `submission_lookup_failed` / `evidence_store_failed_retry` / `evidence_fill_failed_retry` | Transient; retry the same POST. |\n\n⚠ **Unlike `rpa-derm-result`, a bad image here IS a 4xx.** There we accept the result and flag the\nevidence, because rejecting would lose a real county filing. Here the filing is already safely\nrecorded, so refusing a broken attach costs nothing and telling you plainly is more useful.\n\n---\n\n## 4c. `GET /functions/v1/rpa-derm-monthly`: the LWT monthly report\n\n**Added 2026-08-24**, read-only, for the Miami-Dade Liquid Waste Transporter monthly filing.\n\n```\nGET /functions/v1/rpa-derm-monthly?month=YYYY-MM\n```\n\nSame `x-rpa-key`. **No lease, no dispense, no side effects.** Safely re-callable: two calls a second\napart return the same body unless a manifest changed underneath. This is deliberately the opposite of\nthe queue, whose job is to never hand the same work out twice.\n\n**Query params**\n\n| Param | Values | Meaning |\n|---|---|---|\n| `month` | `YYYY-MM` | **required.** Selected on the OFFLOAD date, so a ticket is never split across two reports. |\n| `include` | `all` | Optional. Return every row of every ticket in the month, in-scope or not. Omit for the filing set. |\n\n**Response `200`** (abridged)\n\n```jsonc\n{\n  \"month\": \"2026-06\",\n  \"generated_at\": \"2026-08-25T13:40:00.000Z\",  // fresh every call, EXCLUDED from the ETag\n  \"county\": \"Miami-Dade\",\n  \"scope\": \"picked up in Miami-Dade OR offloaded in Miami-Dade, evaluated per activity\",\n  \"include\": \"in_scope\",\n  \"ticket_count\": 11, \"row_count\": 75, \"excluded_rows\": 10,\n  \"data_quality\": {                       // 🛑 read `checked` BEFORE you file. See below.\n    \"checked\": true,\n    \"conflict_count\": 0,\n    \"conflicts\": []\n  },\n  \"tickets\": [{\n    \"ticket_number\": \"826114\",\n    \"ticket_kind\": \"white\",               // white = Dade offload, yellow = Broward offload\n    \"offload_in_dade\": true,\n    \"offload_date\": \"2026-06-01\",\n    \"disposal_facility\": \"South District WWTP\",\n    \"trucks\": [\"Moises\"],\n    \"truck_decals\": [\"C1184\"],       // NOT index-aligned with trucks\n    \"reported\": false,                // has this ticket been recorded as filed?\n    \"filing\": null,                   // the filing block when reported=true\n    \"excluded_rows\": 0,                   // rows on THIS ticket that fell out of scope\n    \"dade_pickup_gallons\": null,          // null on a white ticket; a block on a yellow one, see §4c\n    \"rows\": [{\n      \"pickup_date\": \"2026-05-28\",        // the VISIT date. see the warning below\n      \"client_code\": \"017-FIA\",\n      \"client_name\": \"Florida Food Eats LLC Fialkoff's (Surfside)\",\n      \"address\": \"9463 Harding Avenue\", \"city\": \"Surfside\",\n      \"state\": \"FL\", \"zip\": \"33154\", \"county\": \"Dade\",\n      \"pickup_in_dade\": true, \"in_scope\": true,\n      \"truck\": \"Moises\", \"truck_capacity_gallons\": 9000,   // INTERNAL fleet fact, do NOT file\n      \"truck_decal\": \"C1184\",             // the vehicle's PERMIT NUMBER. null => refuse the ticket.\n      \"gallons\": null,                    // null on a WHITE ticket (this one). See §4c for yellow.\n      \"gallons_source\": null,             // 'grease_trap_size' | 'service_config_size' | null\n      \"visit_id\": 4636,\n      \"anomaly\": null                     // non-null = this row's dates are impossible\n    }]\n  }]\n}\n```\n\n⚠ **This example is a real payload from a CLOSED month**, fetched live and abridged to one row.\nIt used to show the current month, so its `ticket_count` and `row_count` drifted every time a\nmanifest was filed — they read 12/70 against a live 13/80 within a day. A closed month cannot\ndrift. It also omitted `generated_at`, `data_quality` and `anomaly`, which the endpoint has always\nserved.\n\n**`state` is a USPS two-letter code** (normalised 2026-08-25). It used to be served as whatever we\nheld, which was inconsistently `\"Florida\"` and `\"FL\"`. It is now mapped from an explicit list.\n\n🛑 **If you ever receive a `state` that is NOT two letters, that is us handing you a value we did not\nrecognise, passed through verbatim on purpose so it is visible rather than silently relabelled.\nTreat it as an error and do not print it on the form.** We deliberately do not coerce an unknown\nstate to `FL`, because doing so would put a false state on a county filing for a property that is\ngenuinely somewhere else. Every value we currently hold maps cleanly, so this should never fire.\n\n🛑 **`state` can also be `null`, and null is neither a two-letter code nor a passthrough.** It is\nthe **no-property case**: `address`, `city`, `state`, `zip` and `county` come back **null\ntogether**, never half-populated (measured: 0 rows are half-populated in either direction, an exact\ndiagonal). **14 of 700 rows** as of 2026-08-26, 13 of them in scope, across 10 clients. This was\nasserted in our tests but stated in no document until now, which is our omission and not a change\nin behaviour.\n\n⇒ **What to do with one.** The existing `truck_decal` rule already forces most of these tickets to\nbe refused, which covers 11 of the 13. It does not cover all of them: on the live data exactly\n**one** ticket, `825560`, has rows a fully compliant bot would otherwise file **with a blank pickup\naddress, city and ZIP**. Nothing in `data_quality`, `anomaly` or `excluded_rows` flags the\ncondition, so test `state === null` yourself and refuse the ticket. A county filing with a blank\nservice address is worse than a late one.\n\n**`client_name` has typographic punctuation folded to ASCII** (curly apostrophes become `'`). Accented\nLETTERS are deliberately preserved, because they are the correct spelling of a real name -- you will\nsee `Fendi Château Residences` and addresses on `Española Way`, and those are not encoding errors.\n\n**Rows come back in a total, stable order** (added 2026-08-25): `offload_date`, `ticket_number`,\n`pickup_date`, `visit_id`, `manifest_id`. Two identical calls now return the rows in the same\nsequence, so the `ETag` is meaningful and successive pulls can be diffed.\n\n⚠ **Not byte-identical, and do not compare raw bytes.** Every response carries a fresh\n`generated_at`, so two back-to-back GETs always differ by that one field. It is deliberately\n**excluded from the ETag** — otherwise every call would look like a change. Compare the `ETag`,\nor mask `generated_at` before diffing.\n\n⚠ **This is new.** Until 2026-08-25 the sort had only the first three keys, which left **571 of 690\nrows in a tie group** — the same rows could come back in a different sequence on two identical\ncalls. If you built anything before that date on \"the body is stable\", it was true most of the time\nand not guaranteed. It is guaranteed now.\n\n### 🛑 Scope is evaluated PER ACTIVITY, not per ticket\n\nThe form covers *\"all transportation activities where liquid waste was picked up OR offloaded in\nMiami-Dade County\"*, and an activity is a pickup. Both obvious shortcuts are wrong, and both were\nmeasured over 2026 before this was built:\n\n- filtering on **\"offloaded in Dade\" alone drops 11 tickets and 53 activities**, Broward offloads that\n  carried Miami-Dade pickups;\n- applying the OR at **ticket** grain **over-reports**, because 20 tickets mix counties. Measured on\n  August: ticket `311045` has 0 in-scope rows of 2, `312024` has 3 of 9, `310590` has 6 of 8.\n\nSo the rule is `pickup county = Dade OR the ticket offloaded in Miami-Dade`, applied to each row. If a\nticket offloaded in Dade then every pickup on it qualifies, so only Broward-offload tickets get\ntrimmed.\n\n**By default you receive only in-scope rows**, and a ticket with no in-scope activity is omitted\nentirely. Nothing is dropped silently: `excluded_rows` is reported per ticket and for the month, and\n`?include=all` returns the superset so you can see exactly what was filtered and apply your own rule.\n\n### 🛑 `pickup_date` is the VISIT date, never our `service_date`\n\nOur `derm_manifests.service_date` is a misnomer that holds the **dump** date: the DERM Tracker writes\nthe entered dump date into both columns, so 632 of 669 live manifests have them identical (2026-08-25). This endpoint\nreads the linked visit instead. If you ever see `pickup_date` equal to `offload_date` on every row,\nthat is the bug, not the data.\n\n⚠ **A pickup can fall in the previous month.** Ticket `831710` offloaded 2026-08-02 carrying a\n2026-07-30 pickup. The month selects on the offload date so a ticket is never split in two.\n\n### 🛑 `gallons`: null on a Miami-Dade-offload ticket, a trap CAPACITY per client on a Broward-offload ticket (changed 2026-09-18)\n\n**Until 2026-09-18 this section read \"`gallons` is always `null`, and that is the contract\".** That\nchanged when Yan decided (Slack, 2026-09-18) that Dade pickups disposed in Broward go on the report\nwith **gallons per client**, paying the Dade fee, and Fred chose the Grease Trap Size as the number.\nThe rule now, per row:\n\n| ticket | `gallons` | `gallons_source` |\n|---|---|---|\n| `offload_in_dade: true` (white, Miami-Dade offload) | **always `null`**. Your quantity is the county invoice figure per manifest, as before | `null` |\n| `offload_in_dade: false` (yellow, Broward offload) | the grease trap **capacity** of the visit's property (`properties.grease_trap_size_gallons`, the Jobber \"Grease Trap Size\", two-way synced, edited in the Client App), else the client's Pumping service-config size, else `null` | `grease_trap_size` / `service_config_size` / `null` |\n\n- **It is a capacity, not a measured volume.** We store no measured volume per load (still true). A\n  0 stored anywhere reads as empty. Every non-null value is a positive integer.\n- **Every yellow ticket also carries a ticket-level block**, computed over ALL its Dade-pickup rows\n  (so `include=all` cannot change it):\n\n  ```jsonc\n  \"dade_pickup_gallons\": {\n    \"total\": 1675,          // SUM of gallons over rows with pickup_in_dade = true\n    \"rows\": 4,              // how many such rows\n    \"rows_missing\": 0,      // how many of them have gallons null (no size on record)\n    \"complete\": true        // rows > 0 AND rows_missing = 0. false => DO NOT FILE this ticket yet\n  }\n  ```\n  On a white ticket it is `null`.\n- 🛑 **Rules for your side, in order of how badly it goes wrong if skipped.** (1) SUM the\n  Dade-pickup rows per ticket, or read `total`; never treat \"exactly one distinct value\" as the\n  ticket's figure. (2) A null row makes the ticket **unresolved**: never file a partial sum, and\n  **never fall back to the Miami-Dade decal constant (3,800 / 2,000) on a Broward-disposed ticket**;\n  that number is a Dade load convention and has nothing to do with a Broward dump. (3) What goes on\n  the form is decided by `pickup_in_dade` / `in_scope`, never by whether `gallons` is present: under\n  `include=all` the out-of-scope Broward-pickup rows of a yellow ticket also carry a value.\n- ⚠ **Rows with no size on record will exist until someone types the Grease Trap Size into the\n  Client App** (it pushes to Jobber and this feed reads it live, no redeploy). On 2026-09-18 the\n  August Broward tickets stood at 24 of 31 Dade-pickup rows with a value; only 310607 was complete.\n  Dated observation: the counts move with every edit.\n- ⚠ Two sanity facts you may want to check rather than trust: a trap size can exceed the truck that\n  hauled it (189-FRE, 250 gal on Cloggy, 126 gal), and the Broward receipt's own \"Waste Volume\" is\n  the whole LOAD across every pickup on the ticket (312024: 1,700 gal) and is stored nowhere on our\n  side, so it cannot be split to the Dade rows.\n\n🛑 **CORRECTED 2026-08-26 (kept as history). An earlier version of this section said \"the filed\nquantity is the truck capacity\". That was wrong, and if you built against it, re-check.** Jonathan,\n2026-08-25:\n\n> *\"The county invoice bills actual gallons per manifest — 828837 is 3,800 on the invoice, which as\n> you noted matches no truck — and the form's fee is computed from those gallons. So decal capacity\n> can't fill Quantity.\"*\n\nVerified against our own data: ticket **828837** is Moises, decal **C1184**, and\n`truck_capacity_gallons` **9000** here, against **3,800** billed by the county.\n⇒ **`truck_capacity_gallons` is an INTERNAL FLEET FACT.** Nothing on the county form is computed\nfrom it. It is still served because it is useful for sanity-checking a load, but do not file it.\n⚠ **Corrected again 2026-09-18, in the other direction:** the \"3,800\" on the county invoice is not a\nplant measurement either. The WWTP receipt is hand-written \"approximately 3800 gallons\" for every\nMoises load and \"approximately 2000\" for David, i.e. a per-decal constant, and July 2026 filed as\nexactly 7 x 3,800 + 8 x 2,000 = 42,600 gal. So the Dade quantities are decal constants and the\nBroward quantities are trap capacities; neither is a measured volume. (Moises's tank reads 3,840 on\nour side since the LM11 measurement; the 9000 above is what the fleet table held on 2026-08-26.)\n\n### `truck_decal`, added 2026-08-26: the vehicle's permit number\n\nEvery row now carries **`truck_decal`**, the vehicle's **ACTIVE Miami-Dade** decal, so you can key\non a stable permit number instead of matching on truck names.\n\n```\nMoises → C1184        David → C0976        Cloggy → null        (no truck) → null\n```\n\n⚠ **It does not resolve a quantity on our side.** On a white ticket the quantity is the county\ninvoice figure you resolve by decal (see the corrections above); on a yellow ticket it is the row\n`gallons`. Nothing in this feed is computed from the decal or from TRUCK capacity. The decal answers\n*which permitted vehicle carried this manifest*, and nothing else.\n\nEach ticket also carries **`truck_decals`**, the de-duplicated set for that ticket.\n\n🛑 **`truck_decals` is a MANIFEST summary, not the set of decals belonging on your filing.** It is\nbuilt from **every row on the ticket**, while `rows` is filtered to in-scope rows unless you pass\n`include=all`. So `truck_decals` can name a decal that appears on **no row you were served**.\nLive example, ticket **312024** (August): David ran two Broward pickups on that manifest, both out\nof scope, so every served row is Moises / C1184 while `truck_decals` is `[\"C0976\",\"C1184\"]`.\n**1 of 118 live tickets shows this**, which is exactly the density that survives a spot-check and\nthen breaks a filing.\n⇒ **File from the row's own `truck_decal`.** Use `truck_decals` to answer \"how many trucks touched\nthis manifest\", never \"which permit numbers go on this form\".\n\n⚠ **`truck_decals` is NOT positionally aligned with `trucks`** either. Both are de-duplicated\nindependently, and a truck with no decal appears in one and not the other, so the arrays can differ\nin length. A caller zipping them by index would attribute Cloggy loads to David.\n\n🛑 **`truck_decal` is `null` on 51 of 700 rows and you must REFUSE those tickets, not guess.**\nCloggy (**45 rows, of which 43 are in scope**, across 27 in-scope tickets, offloads 2026-01-15 to\n2026-08-20) holds no decal in **any** jurisdiction, and 6 rows carry no truck at all, so 45 + 6 is\nthe 51. Whether Cloggy is a permitted LWT vehicle at all is an open question with Diego. Never\nsubstitute a capacity, a truck name, or a Broward decal: that would put a wrong permit number on a\ncounty filing.\n\n⚠ *Both figures are given because this sentence previously read \"43 rows\" beside \"51 of 700\", which\nmixes two grains: 51 counts every row, 43 counts only the in-scope ones. `43 + 6 = 49`, so the\narithmetic did not close and a careful reader would have gone looking for two missing rows.*\n\n⚠ **Miami-Dade only, on purpose.** The Broward decal exists in our data (EPD 07058 David, 07675\nMoises) and is deliberately not served. The scope question this used to defer to Yan was answered on\n2026-09-18 (Dade pickups disposed in Broward DO go on the report, with gallons per client, see §4c);\nwhich decal, if any, a Broward-offload row should print is still not decided, so the field stays\nMiami-Dade only until it is.\n\n⚠ **No temporal validity.** If a decal is ever replaced, historical months will report the *current*\none. True today for all four decals, and a real trap the first time a truck is re-permitted.\n\n### 🛑 `data_quality` — read this before you file\n\n**Added 2026-08-24.** Every response carries a `data_quality` block:\n\n```jsonc\n\"data_quality\": {\n  \"checked\": true,          // did the check actually run? see the warning below\n  \"conflict_count\": 1,\n  \"conflicts\": [{\n    \"visit_id\": 6756, \"ticket_number\": \"830673\",\n    \"conflict_kind\": \"pickup_after_offload\",   // or \"visit_not_completed\", or \"both\"\n    \"days_after_offload\": 7, \"violates_guard\": true,\n    \"client_code\": \"175-PV\",\n    \"offload_date\": \"2026-07-22\", \"pickup_date\": \"2026-07-29\",\n    \"visit_status\": \"completed\"\n  }]\n}\n```\n\nand the affected row also carries an inline `anomaly` object (it is `null` on every healthy row).\n\nA conflict means the activity **contradicts itself**: the pickup is dated after its own offload, or\nthe visit backing it is not marked completed. Grease is pumped before it is dumped, so one of the two\nrecords is wrong. **Check it against the paper manifest before filing.**\n\n**These rows are still returned, never filtered or corrected.** Silently clamping a compliance date is\nworse than printing an odd one, and it is your call, not ours. As of 2026-08-24 there are 4 such rows\nin all of 2026 — one each in January, March, May and July.\n\n⚠ **`checked: false` means the check itself failed**, so an empty `conflicts` list proves nothing.\n`conflict_count` is `null` in that case. Do not read zero as an all-clear without `checked: true`.\n\n**Caching:** every response carries a weak `ETag`. Send it back as `If-None-Match` and a repeated poll\nfor the same month costs a `304`.\n\n**Errors**\n\n| Status | Code | Cause |\n|---|---|---|\n| 400 | `month_required_yyyy_mm` | missing or malformed `month` |\n| 400 | `month_out_of_range` | before 2024, or more than ~2 months ahead |\n| 400 | `month_too_large` | the result was truncated by a server row cap. Raises rather than truncating: a short compliance report is the worst failure available. Carries `max_rows` (what you were served) and `total_rows` (what exists), so you can see the size of the gap |\n| 400 | `month_and_unreported_are_exclusive` | you sent both `month=` and `unreported=1`. We refuse rather than silently preferring one |\n| 500 | `monthly_query_failed` | our query for the rows failed. Retry |\n| 500 | `reported_lookup_failed` | our query for filing state failed. Retry. **Nothing is served without it**, because a payload with no `reported` flags would look like \"nothing has been filed\" |\n| 500 | `reported_lookup_truncated` | filing state hit a server row cap, so the unreported set would have been short. Carries `total_tickets` and `returned`. Should not happen below ~1,000 tickets; tell us if you see it |\n\n⚠ **The two `_truncated` / `_too_large` errors exist because a silently short month is the worst\noutcome this endpoint has.** We would rather fail your run than hand you a filing that is missing\nreal transportation activities. Both compare against a true row count rather than against our own\nlimit, because a guard set at the same value as the server's cap can never fire.\n| 401 | `unauthorized` | missing/wrong `x-rpa-key` |\n| 405 | `method_not_allowed` | anything but GET |\n| 500 | `monthly_query_failed` | transient, retry |\n\n---\n\n## 4d. `?unreported=1` and `POST /functions/v1/rpa-derm-monthly-filed`: mark as reported\n\nBuilt 2026-08-26, to your spec. Two pieces: a way to ask **\"what have we not filed yet?\"** without\nguessing at months, and a way to record that a package **was** filed.\n\n### Why it is ticket-grained and not month-grained\n\nYour words, and they are the whole design:\n\n> *\"the flag has to live on the ticket, not on the month ... your `month=` selects on offload date,\n> but our filing window is the invoice package, and packages don't align to months. SP00013840 runs\n> 06/28-07/25. 'All tickets not yet marked reported' replaces that guesswork.\"*\n\nSo `period_start` / `period_end` record the **package's real window**. They are not derived from,\nand must never be reconciled against, the `month=` parameter of the GET.\n\n### `GET /functions/v1/rpa-derm-monthly?unreported=1`\n\nReturns **every in-scope ticket with no real filing recorded**, across all months, ignoring\n`month=` entirely. Same payload shape as the month call, with `\"mode\": \"unreported\"` and\n`\"month\": null`.\n\n⚠ **Sending `month=` and `unreported=1` together is a `400 month_and_unreported_are_exclusive`**,\nnot a silent preference for one. A parameter you supplied being quietly ignored is how you end up\nbelieving you filtered when you did not.\n\nEvery ticket in **both** modes now carries filing state:\n\n```jsonc\n\"reported\": true,\n\"filing\": {\n  \"invoice_id\": \"SP00013840\",\n  \"period_start\": \"2026-06-28\",\n  \"period_end\":   \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"confirmation_ref\": null,          // routinely null, see below\n  \"filed_by_email\": \"fred@ayache.com\"\n}\n```\n\n`\"reported\": false` gives `\"filing\": null`.\n\n### `POST /functions/v1/rpa-derm-monthly-filed`\n\nSame `x-rpa-key`. Call it **after** the form is actually submitted.\n\n```jsonc\n{\n  \"run_id\": \"manual-2026-08-26-sp13840\",   // idempotency key\n  \"tickets\": [\"828837\", \"829216\"],          // ticket_number as served by the GET\n  \"period_start\": \"2026-06-28\",\n  \"period_end\":   \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",  // when YOU filed, not when you call us\n  \"invoice_id\": \"SP00013840\",               // optional\n  \"confirmation_ref\": null,                 // optional, free text, nullable\n  \"filed_by_email\": \"jon.v@ayache.com\",     // required iff run_id starts with \"manual-\"\n  \"dry_run\": true                           // optional, DEFAULTS TO false = a REAL filing\n}\n```\n\n🛑 **`dry_run` is optional and defaults to `false`, which means a real filing.** The check is\n`body.dry_run === true`, so omitting the field, sending `null`, or misspelling it as `dryRun`\n(which is rejected as `unknown_fields`, but only because the allow-list catches it) all record a\n**permanent, real** filing. This example is shown with `true` on purpose: the tickets in it are\nreal ticket numbers, and a copied-and-sent body would mark them filed with a `filed_at` you did\nnot choose. Those tickets then drop out of `?unreported=1` for good and are never filed with the\ncounty. **Set it to `false` only at the moment the form has actually been submitted.**\n\n**Responses**\n\n| | |\n|---|---|\n| `200 {\"recorded\":true, \"filing_id\":…, \"tickets_recorded\":N, \"unknown_tickets\":[…]}` | stored |\n| `200 {\"recorded\":false, \"already_recorded\":true, \"filing_id\":…}` | **replay of a `run_id` we hold. Normal, not an error.** Nothing was changed. |\n| `400` | see the error list below |\n| `401` / `405` | bad key / wrong method |\n\n### The five rules worth knowing\n\n1. 🛑 **Idempotent on `run_id`, enforced by a unique index rather than a look-then-write.** Two\n   concurrent replays cannot both insert: one wins, the other takes the replay path. Retry as often\n   as you like.\n2. 🛑 **Append-only.** There is no update and no delete, and the endpoint's role holds only\n   `SELECT` + `INSERT`, so the inability is a **grant**, not a branch. A record of \"we told the\n   county this\" must not be machine-rewritable.\n   *This sentence was written on 2026-08-26 and was not true when written.* The creating migration\n   issued `REVOKE ALL ... FROM PUBLIC`, which does not touch a grant held by a named role, while\n   `CREATE TABLE` had already handed out full privileges through Supabase's default privileges. So\n   both tables carried `UPDATE`, `DELETE` and `TRUNCATE` for `service_role` **and** for\n   `authenticated`, with RLS off. Corrected the same day in\n   `2026-08-26_1815_lwt_filings_lock_down.sql`, which revokes those, gives `authenticated` nothing,\n   enables RLS, and adds the audit triggers the tables should have had. Now measured live as\n   `service_role: SELECT+ INSERT+ UPDATE- DELETE- TRUNCATE-`, and probed as the role itself inside\n   a transaction that cannot commit.\n3. ⚠ **An unknown ticket is RECORDED, not rejected, and echoed back in `unknown_tickets`.** Your\n   words: *\"any difference against the invoice becomes a finding in either direction, including the\n   reverse case we can't detect today.\"* Refusing it would destroy exactly that finding. A\n   non-empty `unknown_tickets` means you filed something we have no manifest for: worth a look, not\n   an error.\n4. ⚠ **`confirmation_ref` is free text and nullable, and null is the normal case.** Your words:\n   *\"the county gives no confirmation number for the monthly form. None of the six filed months has\n   one.\"* There is no format check and nothing infers a failure from its absence.\n5. ⚠ **`dry_run: true` records the filing but never marks a ticket reported.** A rehearsal must not\n   make a real ticket look filed.\n\n⚠ **`filed_by_email` must be present exactly when `run_id` starts with `manual-`**, and absent\notherwise. Same rule `derm_portal_submissions` uses, so the two markers can never disagree about\nwhether a person did this. Violating it is a `400 actor_markers_disagree`, and the database carries\nthe same CHECK, so it cannot be bypassed.\n\n**`400` errors:** `unknown_fields`, `invalid_run_id`, `tickets_required`, `too_many_tickets`,\n`invalid_ticket_numbers`, `invalid_period_start`, `invalid_period_end`,\n`period_end_before_period_start`, `invalid_filed_at`, `actor_markers_disagree`, `invalid_json`,\n`invalid_body`.\n\n**`500` errors**, both of which mean **nothing was recorded**, so retrying is always safe:\n\n| | |\n|---|---|\n| `server_misconfigured` | no API key is configured on our side. Not something you can fix; tell us |\n| `record_failed` | the write failed. Carries `detail`. **Retry with the SAME `run_id`** |\n\n✅ **The write is ATOMIC, so a `500` can never leave a half-recorded filing** (changed\n2026-08-27). The filing and its tickets are written by one database function in one transaction:\neither both land or neither does. So a failure never claims your `run_id`, and a retry with the\nsame `run_id` is a clean first attempt rather than a replay of something broken.\n\n⚠ **This was not true before 2026-08-27 and the difference matters if you saw the old docs.** The\ntwo writes used to be two separate calls, which is two transactions, and the first one is what\nclaims the `run_id`. A failure on the second therefore left the `run_id` burned and the filing\n**permanently empty**: retrying returned `already_recorded: true` with `tickets_recorded: 0` and\ninserted nothing. It never fired in practice, and it can no longer happen, but if you built a\nworkaround for it you can drop it.\n\n⇒ **`tickets_recorded: 0` on a replay should now be impossible.** If you ever see it, something is\nwrong that we do not understand: tell us rather than working around it.\n\n### What this deliberately is NOT\n\nOption 2, not option 3. **No per-ticket status enum, no partial-filing model, no correction\nworkflow, no resubmission state.** A ticket is either recorded on a real filing or it is not. A\nrefile is simply a second filing that includes it, and `reported` stays an existence question.\nIf you need option 3, say so and it is a new migration, not a column bolted onto this.\n\n---\n\n## 5. How you run it — you poll, on your own schedule\n\n**You own the timing. There is no push from us** — you don't expose any endpoint, and there is no\nsecond \"run\" key. Just poll the queue and file whatever it returns:\n\n- Poll `GET /rpa-derm-queue` on a **slow schedule** (every 15–30 min is plenty), file each report,\n  and POST the result. That's the whole loop.\n- Reporting is safe to run as often as you like: the queue only hands you visits that still need\n  filing (a `SUCCESS` permanently removes one), each dispensed visit is **leased for 20h** so a crash\n  can't get it re-handed-out, and `rpa-derm-result` is **idempotent on `(visit_id, gdo_id, run_id)`**, so a\n  duplicate or retried run never double-files.\n- Nothing on our side calls you, so you never need an inbound endpoint or a public URL.\n\n---\n\n## 6. What the result feeds (status surfaces)\n\nYour `POST` results drive per-visit \"GDO report filed / pending / failed\" chips in our apps:\n\n- `derm.gdo_report_status` — per visit, shown in the internal DERM Tracker.\n- `customer.gdo_reports` — per visit, shown on the customer's Field Portal.\n\nEvery write from the bot is attributed in our audit trail as `app_source = 'gdo-report-bot'`, and every\nresult is logged in `public.derm_portal_submissions` (visit_id / run_id / status / confirmation /\nscreenshot path / dry_run).\n\n---\n\n## 7. Error reference\n\nAll errors are `{\"error\":\"<code>\"}` with the HTTP status shown.\n\n**Both endpoints**\n\n| Status | Code | Cause |\n|---|---|---|\n| 401 | `unauthorized` | Missing/wrong `x-rpa-key`. |\n| 405 | `method_not_allowed` | Wrong HTTP method. |\n| 503 | `service_not_configured` | Key secret not set on our side (tell us). |\n\n**`rpa-derm-queue`**\n\n| Status | Code |\n|---|---|\n| 500 | `queue_query_failed` |\n\n**`rpa-derm-result`**\n\n| Status | Code | Cause |\n|---|---|---|\n| 400 | `invalid_json` | Body was not JSON. |\n| 400 | `unknown_field_<name>` | A field we do not accept. |\n| 400 | `visit_id_required_integer` | Missing/invalid `visit_id`. |\n| 400 | `manifest_id_must_be_integer_or_omitted` | Bad `manifest_id`. |\n| 400 | `run_id_must_be_alnum_dot_dash_underscore_max100` | Bad `run_id` charset/length. |\n| 400 | `status_must_be_short_uppercase_code` | `status` failed `^[A-Z0-9_]{1,64}$`. |\n| 400 | `retryable_boolean_required` | `retryable` not a boolean. |\n| 400 | `failure_reason_max_1000_chars` | Too long. |\n| 400 | `portal_confirmation_max_200_chars` | Too long. |\n| 400 | `attempted_at_must_be_iso8601` | Not a valid ISO timestamp. |\n| 400 | `screenshot_missing_reason_max_300_chars` | Too long. |\n| 400 | `screenshot_or_screenshot_missing_reason_required` | Neither provided. |\n| 400 | `success_requires_portal_confirmation` | `SUCCESS` without a `portal_confirmation`. |\n| 422 | `visit_not_found_or_deleted` | `visit_id` unknown or soft-deleted. |\n| 500 | `visit_lookup_failed` / `result_store_failed_retry` | Transient; retry the same POST. |\n\n---\n\n## 8. Rollout gates\n\n✅ **Gates 1 and 2 are PASSED.** The dry-run pass ran, and real reports have been filed and confirmed\non the county side: **7 confirmed filings between 2026-07-24 and 2026-08-17**.\n\n3. Gate 3, **opening it for all** the eligible clients, is the remaining one and is Fred's call.\n\n---\n\n## 9. Operating notes (our side)\n\n- **Watchdog:** a daily health check surfaces a stuck queue / repeated data errors / storage failures /\n  a code-27 visit whose service is not DERM-manifest-requiring (a mis-applied add-on to flag).\n- **Launch cutoff:** currently `2026-07-21` bounds the live queue.\n- **Evidence retention:** screenshots live in the private `rpa-evidence` bucket keyed `visit_id/run_id.jpg`.\n- **PII note (under review):** the queue currently signs the **raw multi-client** address sheet. This is\n  intended because county submission needs the real document, but if the portal only needs the client's\n  own row we will serve the redacted copy instead — pending confirmation of what the portal requires.\n\n---\n\n## 10. Integration checklist (what we still need from you)\n\n1. **The CSV column list** you read today, so we can flag any field name you expect that we do not\n   already return.\n2. Your **repo link** (we set up a private repo under the `webunclogmecom` org; Railway auto-deploys\n   from `main`).\n\nWe send the key and the county portal login privately. You expose nothing to us — you poll (section 5).\n\n---\n\n## 11. Using this Postman collection\n\nThe collection in this folder (`gdo-reporting-bot.postman_collection.json`) is a ready-to-run test\nharness for the two endpoints above.\n\n🛑 **The collection's Overview description IS this file, generated, never hand-edited (since\n2026-09-18).** From 2026-07-23 to 2026-09-18 it was a pasted copy that nobody re-pasted, and it opened\nwith a \"FROZEN SNAPSHOT, OUT OF DATE\" banner while this README moved eleven times. Now\n`node scripts/postman/sync_collection_description.js` writes this README into `info.description`\n(one line of the JSON changes, nothing else), `--check` refuses a stale copy, and both the pre-commit\nhook (`.githooks/pre-commit`, enabled with `git config core.hooksPath .githooks`) and the\n`postman-doc-drift` workflow run that check together with `scripts/checks/api-doc-drift.js`. **On a\npush to `main` the same workflow then pushes the collection into the Postman workspace through the\nPostman API and reads it back** (`scripts/postman/publish_collection.js`, key in the repository secret\n`POSTMAN_API_KEY`, collection uid pinned in the workflow), so the copy you open in Postman is the\nrepo's within a minute of the push, under a stable uid. So the routine after editing this file is:\nrun the sync script, commit the collection with the README, push. No re-import. ⚠ A push replaces the\ncollection definition, so the INITIAL value of `rpaBotKey` is blank after every push: keep the key in\nthe **`UnclogMe - RPA (Prod)` environment** (never touched by the push) and select it when you run.\nRelative links do not resolve inside Postman; the GitHub copy does.\n\n**Setup (once):** right-click the collection **UnclogMe - GDO Online Reporting Bot API** → **Edit** →\n**Variables** tab → paste your key into `rpaBotKey`'s **Current value** column → **Save**. The value is\n`RPA_BOT_KEY` in `Supabase/.env` (never commit it). Auth is one collection-level header\n`x-rpa-key: {{rpaBotKey}}`, so every request uses it automatically. (An optional\n`UnclogMe - RPA (Prod)` environment is also included; if selected it overrides the collection value.)\n\n**Run:** top to bottom. Start with **1. Queue → Queue - dry-run** — it returns real historical code-27\nvisits and captures the first `visit_id` into a collection variable the Result requests reuse. Then run\n**2. Result** (all `dry_run:true`, so they record separately and never affect the live queue or what\ncustomers/apps see), and **3. Validation** (shows the endpoint rejecting bad input).\n\n**Variables:** you only ever set `rpaBotKey`. `baseUrl` and `wrongRpaKey` are preset. `dryRunVisitId` /\n`dryRunManifestId` / `evidenceRunId` are auto-captured into **collection** scope by the requests that\ncreate them.\n\n🛑 **`wrongRpaKey` exists so the \"Wrong key -> 401\" test does NOT hold a literal in its auth field, and\nit must stay that way.** Postman's secret scanner flags **any** hardcoded value in an auth field as an\n\"Auth secret\", whatever the string is: it flagged `deliberately-wrong-key` and then flagged the\nharmless rename `WRONG-ON-PURPOSE` identically. That prompt **blocks the whole collection import**\nbehind Vault / Secure / Remove, none of which suit a value that is deliberately invalid. A `{{variable}}`\nis never flagged, which is why the collection's own auth (`{{rpaBotKey}}`) always imported cleanly.\n⚠ Put a literal back in that field and every future import, including John's, hits the prompt again.\n\n**⚠ Importing by URL silently does nothing.** Pasting the raw GitHub URL into Import produced\n\"Import Failed\" (and once, \"Import Complete\" and \"Import Failed\" at the same time) while changing\nnothing. **Paste the JSON contents instead**, which works first time. Verify by looking at the folder\ntree, never at the notification — leave them empty and do **not** add\nthem to a selected environment (an empty environment copy shadows the captured value and blanks the\nResult requests).\n\n**Gotcha:** a `401 unauthorized` means `rpaBotKey` is still blank (or an empty environment is selected\nand overriding it). Dry-run result writes create harmless `dry_run` rows in our log table; clear anytime\nwith `DELETE FROM public.derm_portal_submissions WHERE dry_run;`. There is deliberately no\n\"SUCCESS needs confirmation\" test here: that rule (and the double-file guards) apply only to LIVE\nreports, so a dry-run `SUCCESS` is exempt and cannot be exercised against dry-run data.\n\n### Folder 6 writes to Prod, on purpose, and here is exactly what it leaves\n\nFolder 6 exercises `rpa-derm-monthly-filed`, which is a **write** endpoint. Every request in it\nsends `dry_run: true` and uses a **stable `run_id`**, so re-running the folder for ever creates\nexactly **two** rows in `public.lwt_filings` (`bot-postman-dryrun`, `bot-postman-unknown`) rather\nthan one per run. Both are `dry_run = true`.\n\n🛑 **A dry-run filing can never mark a ticket reported.** `derm.v_lwt_ticket_reported` resolves a\nticket's filing through a `LEFT JOIN LATERAL` whose `WHERE` carries `lf.dry_run = false`, so the\nexclusion is structural rather than a branch someone can forget. Verified after a full folder run:\n`?unreported=1` returned **118 tickets before and 118 after**, and all 127 tickets in the view read\n`reported = false`. Folder 6 asserts this itself, by re-reading the unreported list after the write.\n\n⚠ The table is **append-only** and the endpoint's role holds only `SELECT` + `INSERT`, so unlike\nthe `derm_portal_submissions` note above **there is no cleanup statement to offer you**. The two\nrows are meant to stay. That is the cost of testing a write endpoint honestly, and it is bounded at\ntwo rows because the `run_id` never rotates. (Both properties became true on 2026-08-26 in\n`2026-08-26_1815_lwt_filings_lock_down.sql`; see rule 2 above for what they were before. The\ntwo-row bound also depended on the wrong-key test, which was silently sending the **valid** key and\nwould have written a third row on its first real run: fixed the same day.)\n\n⚠ **A third row briefly existed on 2026-08-26 and was removed the same day, by decision rather than\nby habit.** `bot-postman-badkey` was written by our own verification harness, which modelled\nPostman's apikey signer backwards and so sent the valid key to the wrong-key request. It was\n`dry_run = true` and marked no ticket reported, and the collection as shipped cannot produce it.\nDeleted with Fred's explicit approval, pinned to the primary key while re-asserting the predicate\nthat made it deletable, and recoverable two ways: a JSON backup, and `audit.logs.old_row` for both\nthe filing and its cascaded ticket row. **`service_role` could not have done it** (the endpoint's\nrole holds no DELETE); it took the table owner over the Management API, which is what append-only\nis supposed to mean.\n\n### What the collection does and does not exercise\n\nEvery error code either has a request that produces it, or appears in the table below. **There is no\nthird category**, so a code missing from both is drift and\n`node scripts/checks/api-doc-drift.js` will say so.\n\n| endpoint | exercised | not exercised |\n|---|---|---|\n| `rpa-derm-monthly` | 6 of 10 | 4 |\n| `rpa-derm-monthly-filed` | 12 of 14 | 2 |\n\n**The 6 that are deliberately not exercised, and why none of them can be:**\n\n| code | why no request can produce it |\n|---|---|\n| `month_too_large` | needs a month over the 1,000-row server cap. Largest real month is 109 rows |\n| `reported_lookup_truncated` | needs over 1,000 distinct tickets. There are 127 |\n| `monthly_query_failed` | a database failure |\n| `reported_lookup_failed` | a database failure |\n| `record_failed` | a database failure |\n| `server_misconfigured` | needs the API key secret to be unset on our side |\n\n⚠ **Three of those are the ones you would most want tested**, since they are the truncation and\nwrite-failure paths that decide whether a short filing reaches the county. They are listed here\nrather than quietly absent so the gap stays visible. Testing them properly means fault injection,\nwhich is a bigger change than this collection.\n\n✅ **The worst of them is gone rather than untested.** `ticket_insert_failed` used to sit in this\nlist and was the only one that could leave a permanently broken record. It no longer exists: the\nwrite is atomic as of 2026-08-27, so the failure it named cannot occur. **The atomicity itself IS\ntested**, in `2026-08-27_1024_lwt_record_filing_atomic.sql`, by forcing the ticket insert to fail\nand asserting the filing row does not survive, with a deliberately non-atomic control proving the\nprobe can detect one that does.\n\n⚠ **Every negative test asserts the error CODE, never just the status.** A request that fails at an\nearlier validation gate still returns `400`, so a status-only assertion passes while testing\nsomething entirely different. Validation runs in a fixed order (json, body shape, unknown fields,\n`run_id`, actor markers, periods, period order, `filed_at`, tickets present, ticket count, ticket\nformat), and each request is built valid up to the gate it targets.\n\n⚠ **Run \"5. Monthly - filing set\" first.** It captures `{{lwtTicket}}`, which folder 6 files\nagainst. Running folder 6 cold files against the literal string `{{lwtTicket}}`, which the endpoint\ncorrectly rejects as an invalid ticket number.\n\n⚠ **One assertion in folder 6 goes inert after the first run ever.** The `unknown_tickets` echo can\nonly be checked on the run that actually records the row; every later run takes the replay path,\nwhich returns no `unknown_tickets` to inspect. The request logs a `NOTE` when it skips, and the\nhalf that matters (an unknown ticket is **accepted**, never rejected) runs every time. To re-arm\nthe echo check, bump the `run_id` suffix in that request's body once.\n"
  },
  "auth": {
    "type": "apikey",
    "apikey": [
      {
        "key": "key",
        "value": "x-rpa-key",
        "type": "string"
      },
      {
        "key": "value",
        "value": "{{rpaBotKey}}",
        "type": "string"
      },
      {
        "key": "in",
        "value": "header",
        "type": "string"
      }
    ]
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "https://wbasvhvvismukaqdnouk.supabase.co/functions/v1",
      "type": "string"
    },
    {
      "key": "rpaBotKey",
      "value": "",
      "type": "string"
    },
    {
      "key": "dryRunVisitId",
      "value": "",
      "type": "string"
    },
    {
      "key": "dryRunManifestId",
      "value": "",
      "type": "string"
    },
    {
      "key": "evidenceRunId",
      "value": "",
      "type": "string"
    },
    {
      "key": "wrongRpaKey",
      "value": "invalid",
      "type": "string"
    },
    {
      "key": "reportMonth",
      "value": "",
      "type": "string"
    },
    {
      "key": "monthlyEtag",
      "value": "",
      "type": "string"
    },
    {
      "key": "lwtTicket",
      "value": "",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "1. Queue (read) - no side effects",
      "item": [
        {
          "name": "Queue - dry-run (START HERE)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-queue?mode=dryrun",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-queue"
              ],
              "query": [
                {
                  "key": "mode",
                  "value": "dryrun"
                }
              ]
            },
            "description": "Up to 25 REAL historical code-27 visits to report, keyed by visit_id, each with client + service fields, gdo_number, and 4h signed document URLs. Captures the first visit_id/manifest_id for the Result requests. Read-only.\n\n--- 2026-08-11: ONE ROW PER (TICKET, PERMIT) ---\nA client with N active GDO permits owes Miami-Dade N reports per qualifying dump ticket (we pump both traps). Each row now carries gdo_number for the permit THAT ROW IS FOR, plus a new gdo_id column.\nThe queue serves at most ONE permit per ticket per batch: the 20h manifest-grained lease holds the rest until the served one is filed. So a multi-permit ticket appears again on a later poll with its next permit, and a three-permit client files over about two days. Nothing about the 25-row cap changes.\nYou do NOT need to send the permit back. We resolve it server-side from the pair we served.\n\n--- 2026-09-18: THE SAMPLE NOW COVERS EVERY JURISDICTION ---\nDry-run used to serve the 25 OLDEST historical rows, all Miami-Dade, so a Broward yellow ticket (jurisdiction \"broward\", disposal_facility \"Water and Wastewater Services\", white_manifest_number null, file ticket_number) never appeared here. It is now newest-first within each jurisdiction and interleaved across them, still capped at 25, still no lease. A sample, not a queue: do not build on its order. Live mode is unchanged (oldest first, leased).\n(The 2026-08-11 one-permit-per-ticket paragraph above is SUPERSEDED since 2026-08-31: every permit on a ticket is served in one batch and the work key is (visit_id, gdo_id). README section 3 is current.)"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const j = pm.response.json();",
                  "pm.test('200 OK', () => pm.response.to.have.status(200));",
                  "pm.test('mode = dryrun', () => pm.expect(j.mode).to.eql('dryrun'));",
                  "pm.test('reports is an array', () => pm.expect(j.reports).to.be.an('array'));",
                  "if (j.reports && j.reports.length) {",
                  "  const r = j.reports[0];",
                  "  pm.collectionVariables.set('dryRunVisitId', r.visit_id);",
                  "  pm.collectionVariables.set('dryRunManifestId', r.manifest_id);",
                  "  pm.test('report keyed by visit_id, has gdo + signed docs', () => {",
                  "    pm.expect(r.visit_id, 'visit_id').to.be.a('number');",
                  "    pm.expect(r, 'gdo_number').to.have.property('gdo_number');",
                  "    pm.expect(r.documents, 'documents.address').to.have.property('address');",
                  "  });",
                  "  console.log('Captured dryRunVisitId=' + r.visit_id + '  (' + r.client_code + ' ' + r.client_name + ', service_date ' + r.service_date + ')');",
                  "} else { console.warn('No dry-run reports returned.'); }"
                ]
              }
            }
          ]
        },
        {
          "name": "Queue - live",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-queue",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-queue"
              ]
            },
            "description": "The real live queue of code-27 visits awaiting their online report. Empty (count 0) until launch. A visit stays here until the county confirms its report. Read-only.\n\n--- 2026-08-11: ONE ROW PER (TICKET, PERMIT) ---\nA client with N active GDO permits owes Miami-Dade N reports per qualifying dump ticket (we pump both traps). Each row now carries gdo_number for the permit THAT ROW IS FOR, plus a new gdo_id column.\nThe queue serves at most ONE permit per ticket per batch: the 20h manifest-grained lease holds the rest until the served one is filed. So a multi-permit ticket appears again on a later poll with its next permit, and a three-permit client files over about two days. Nothing about the 25-row cap changes.\nYou do NOT need to send the permit back. We resolve it server-side from the pair we served."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const j = pm.response.json();",
                  "pm.test('200 OK', () => pm.response.to.have.status(200));",
                  "pm.test('mode = live', () => pm.expect(j.mode).to.eql('live'));",
                  "console.log('Live queue count = ' + j.count + '  (0 until launch: only code-27 visits serviced on/after the cutoff appear).');"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "2. Result (write) - dry-run safe",
      "item": [
        {
          "name": "Report result - retryable failure",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-result",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-result"
              ]
            },
            "description": "Logs a retryable failure (e.g. portal timeout) for the captured visit. Fresh GUID run_id each send. dry_run:true keeps it out of the live pipeline. Expect 201.\n\n--- 2026-08-11: RESPONSE NOW REPORTS WHETHER THE EVIDENCE LANDED ---\nEvery 200/201 now also returns:\n  screenshot_stored          boolean, true only if the image is actually in the bucket\n  screenshot_missing_reason  our drop reason when it is not (SCREENSHOT_TOO_LARGE, SCREENSHOT_DECODE_FAILED, STORE_FAILED), else null\nAn oversize, undecodable or unstorable image is still recorded as a filing and still answers 201: a real county filing must never be lost over a screenshot problem. You can now SEE the drop instead of having to infer it.\nOn a deduped 200 these describe the STORED row, not what you just posted, because a dedupe uploads nothing.\n\ngdo_id and gdo_number are ACCEPTED AND IGNORED. You do not need to send them; we resolve the permit ourselves. They are accepted only so a bot built against the earlier draft contract cannot 400 on a filing that already happened.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": {{dryRunVisitId}},\n  \"manifest_id\": {{dryRunManifestId}},\n  \"run_id\": \"{{$guid}}\",\n  \"status\": \"ERROR_PORTAL_TIMEOUT\",\n  \"retryable\": true,\n  \"attempted_at\": \"{{$isoTimestamp}}\",\n  \"failure_reason\": \"Postman test - portal timed out\",\n  \"screenshot_missing_reason\": \"no screenshot in test\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Run \"Queue - dry-run\" first to capture a visit', function () {",
                  "  const v = pm.collectionVariables.get('dryRunVisitId');",
                  "  pm.expect(v, 'dryRunVisitId (run the dry-run queue first)').to.be.a('number');",
                  "});",
                  "const j = pm.response.json();",
                  "pm.test('recorded (201 new or 200 dedupe)', () => pm.expect([200,201]).to.include(pm.response.code));",
                  "pm.test('recorded flag true', () => pm.expect(j.recorded).to.eql(true));",
                  "console.log('recorded id=' + j.id + '  deduped=' + j.deduped);",
                  "pm.test('screenshot_stored is false when no image was sent', function () {",
                  "  const r = pm.response.json();",
                  "  pm.expect(r).to.have.property('screenshot_stored');",
                  "  pm.expect(r.screenshot_stored).to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Report result - SUCCESS + screenshot",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-result",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-result"
              ]
            },
            "description": "Logs a confirmed report with portal_confirmation and a tiny base64 JPEG screenshot - exercises the private evidence bucket. dry_run:true. Expect 201. In production the bot sends the real portal screenshot bytes here.\n\n--- 2026-08-11: RESPONSE NOW REPORTS WHETHER THE EVIDENCE LANDED ---\nEvery 200/201 now also returns:\n  screenshot_stored          boolean, true only if the image is actually in the bucket\n  screenshot_missing_reason  our drop reason when it is not (SCREENSHOT_TOO_LARGE, SCREENSHOT_DECODE_FAILED, STORE_FAILED), else null\nAn oversize, undecodable or unstorable image is still recorded as a filing and still answers 201: a real county filing must never be lost over a screenshot problem. You can now SEE the drop instead of having to infer it.\nOn a deduped 200 these describe the STORED row, not what you just posted, because a dedupe uploads nothing.\n\ngdo_id and gdo_number are ACCEPTED AND IGNORED. You do not need to send them; we resolve the permit ourselves. They are accepted only so a bot built against the earlier draft contract cannot 400 on a filing that already happened.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": {{dryRunVisitId}},\n  \"manifest_id\": {{dryRunManifestId}},\n  \"run_id\": \"succ-{{$guid}}\",\n  \"status\": \"SUCCESS\",\n  \"retryable\": false,\n  \"attempted_at\": \"{{$isoTimestamp}}\",\n  \"portal_confirmation\": \"TEST-CONF-{{$timestamp}}\",\n  \"screenshot\": \"/9j/4AAQSkZJRgAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICAgICD/2Q==\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Run \"Queue - dry-run\" first to capture a visit', function () {",
                  "  const v = pm.collectionVariables.get('dryRunVisitId');",
                  "  pm.expect(v, 'dryRunVisitId (run the dry-run queue first)').to.be.a('number');",
                  "});",
                  "const j = pm.response.json();",
                  "pm.test('recorded (201 new or 200 dedupe)', () => pm.expect([200,201]).to.include(pm.response.code));",
                  "pm.test('recorded flag true', () => pm.expect(j.recorded).to.eql(true));",
                  "console.log('recorded id=' + j.id + '  deduped=' + j.deduped);",
                  "pm.test('screenshot_stored reports the image landed', function () {",
                  "  pm.expect(j).to.have.property('screenshot_stored');",
                  "  pm.expect(j.screenshot_stored, 'a valid JPEG was posted, so it must have stored').to.eql(true);",
                  "  pm.expect(j.screenshot_missing_reason, 'stored images carry no drop reason').to.eql(null);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Report result - dedupe demo (send twice)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-result",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-result"
              ]
            },
            "description": "FIXED run_id 'postman-dedupe-1'. Send once -> 201 {deduped:false}, again -> 200 {deduped:true}: idempotency on (visit_id, run_id) so a retried POST never double-records. (If you already ran it, both are 200 until the dry_run rows are cleared.)\n\n--- 2026-08-11: RESPONSE NOW REPORTS WHETHER THE EVIDENCE LANDED ---\nEvery 200/201 now also returns:\n  screenshot_stored          boolean, true only if the image is actually in the bucket\n  screenshot_missing_reason  our drop reason when it is not (SCREENSHOT_TOO_LARGE, SCREENSHOT_DECODE_FAILED, STORE_FAILED), else null\nAn oversize, undecodable or unstorable image is still recorded as a filing and still answers 201: a real county filing must never be lost over a screenshot problem. You can now SEE the drop instead of having to infer it.\nOn a deduped 200 these describe the STORED row, not what you just posted, because a dedupe uploads nothing.\n\ngdo_id and gdo_number are ACCEPTED AND IGNORED. You do not need to send them; we resolve the permit ourselves. They are accepted only so a bot built against the earlier draft contract cannot 400 on a filing that already happened.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": {{dryRunVisitId}},\n  \"run_id\": \"postman-dedupe-1\",\n  \"status\": \"ERROR_PORTAL_TIMEOUT\",\n  \"retryable\": true,\n  \"attempted_at\": \"{{$isoTimestamp}}\",\n  \"screenshot_missing_reason\": \"dedupe demo\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Run \"Queue - dry-run\" first to capture a visit', function () {",
                  "  const v = pm.collectionVariables.get('dryRunVisitId');",
                  "  pm.expect(v, 'dryRunVisitId (run the dry-run queue first)').to.be.a('number');",
                  "});",
                  "const j = pm.response.json();",
                  "pm.test('200 or 201', () => pm.expect([200,201]).to.include(pm.response.code));",
                  "console.log(pm.response.code === 201 ? 'First send -> 201 recorded (deduped=false)' : 'Repeat send -> 200 deduped=' + j.deduped);"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "3. Validation (expected 4xx)",
      "item": [
        {
          "name": "Bad run_id -> 400",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-result",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-result"
              ]
            },
            "description": "run_id 'bad/slash' contains '/', rejected because run_id becomes part of a storage object key. Expect 400.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": {{dryRunVisitId}},\n  \"run_id\": \"bad/slash\",\n  \"status\": \"ERROR_PORTAL_TIMEOUT\",\n  \"retryable\": true,\n  \"attempted_at\": \"{{$isoTimestamp}}\",\n  \"screenshot_missing_reason\": \"x\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Run \"Queue - dry-run\" first to capture a visit', function () {",
                  "  const v = pm.collectionVariables.get('dryRunVisitId');",
                  "  pm.expect(v, 'dryRunVisitId (run the dry-run queue first)').to.be.a('number');",
                  "});",
                  "pm.test('400 rejected', () => pm.response.to.have.status(400));",
                  "pm.test('error names run_id', () => pm.expect(pm.response.json().error).to.include('run_id'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Unknown field -> 400",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-result",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-result"
              ]
            },
            "description": "Sends an unexpected body field; unknown fields are rejected so a typo cannot silently no-op. Expect 400.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": {{dryRunVisitId}},\n  \"run_id\": \"unknown-{{$guid}}\",\n  \"status\": \"ERROR_PORTAL_TIMEOUT\",\n  \"retryable\": true,\n  \"attempted_at\": \"{{$isoTimestamp}}\",\n  \"screenshot_missing_reason\": \"x\",\n  \"dry_run\": true,\n  \"totally_made_up_field\": 1\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Run \"Queue - dry-run\" first to capture a visit', function () {",
                  "  const v = pm.collectionVariables.get('dryRunVisitId');",
                  "  pm.expect(v, 'dryRunVisitId (run the dry-run queue first)').to.be.a('number');",
                  "});",
                  "pm.test('400 rejected', () => pm.response.to.have.status(400));",
                  "pm.test('error names the unknown field', () => pm.expect(pm.response.json().error).to.include('unknown_field'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Wrong key -> 401",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-queue",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-queue"
              ]
            },
            "description": "Overrides the collection auth with a wrong x-rpa-key to show the endpoint is secured. Expect 401.",
            "auth": {
              "type": "apikey",
              "apikey": [
                {
                  "key": "key",
                  "value": "x-rpa-key",
                  "type": "string"
                },
                {
                  "key": "value",
                  "value": "{{wrongRpaKey}}",
                  "type": "string"
                },
                {
                  "key": "in",
                  "value": "header",
                  "type": "string"
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('401 unauthorized', () => pm.response.to.have.status(401));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "4. Evidence (late attach) - dry-run safe",
      "item": [
        {
          "name": "Evidence 1 - result with NO image (sets evidenceRunId)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-result",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-result"
              ]
            },
            "description": "Posts a result with NO screenshot and an explicit screenshot_missing_reason. This is the shape the bot uses when the county confirmation email has not arrived yet: the filing is recorded straight away and the evidence follows.\n\nRun \"1. Queue - dry-run\" first so dryRunVisitId is captured. This request generates evidenceRunId and the rest of the folder reuses it.\n\nExpect 201 with screenshot_stored:false.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": \"{{dryRunVisitId}}\",\n  \"run_id\": \"{{evidenceRunId}}\",\n  \"status\": \"SUCCESS\",\n  \"retryable\": false,\n  \"attempted_at\": \"{{$isoTimestamp}}\",\n  \"portal_confirmation\": \"Submitted (Postman test, no tracking number)\",\n  \"screenshot_missing_reason\": \"EMAIL_PENDING\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.collectionVariables.set('evidenceRunId', 'pm-evidence-' + Date.now().toString(36));"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const j = pm.response.json();",
                  "pm.test('201 created', () => pm.response.to.have.status(201));",
                  "pm.test('recorded without evidence', () => {",
                  "  pm.expect(j.recorded, 'recorded').to.eql(true);",
                  "  pm.expect(j.screenshot_stored, 'screenshot_stored').to.eql(false);",
                  "  pm.expect(j.screenshot_missing_reason, 'reason').to.eql('EMAIL_PENDING');",
                  "});",
                  "console.log('Result stored with evidence pending, run_id=' + pm.collectionVariables.get('evidenceRunId'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Evidence 2 - attach the image",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-evidence",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-evidence"
              ]
            },
            "description": "Attaches the evidence to the result posted above. The image here is a 1x1 PNG, deliberately, to show that the stored extension follows the REAL BYTES: the path comes back .png even though the older contract only ever wrote .jpg.\n\nExpect 200 with attached:true, and cleared_missing_reason echoing the EMAIL_PENDING that was on the row.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": \"{{dryRunVisitId}}\",\n  \"run_id\": \"{{evidenceRunId}}\",\n  \"screenshot\": \"iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const j = pm.response.json();",
                  "pm.test('200 OK', () => pm.response.to.have.status(200));",
                  "pm.test('attached, and the reason was cleared', () => {",
                  "  pm.expect(j.attached, 'attached').to.eql(true);",
                  "  pm.expect(j.screenshot_stored, 'screenshot_stored').to.eql(true);",
                  "  pm.expect(j.cleared_missing_reason, 'cleared_missing_reason').to.eql('EMAIL_PENDING');",
                  "});",
                  "pm.test('extension follows the bytes (.png)', () => pm.expect(j.screenshot_path).to.match(/\\.png$/));"
                ]
              }
            }
          ]
        },
        {
          "name": "Evidence 3 - attach again (fill-once, expect attached:false)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-evidence",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-evidence"
              ]
            },
            "description": "Sends the exact same attach a second time. FILL-ONCE: this endpoint can only turn \"no image\" into \"an image\", never replace one. A repeat is a harmless no-op, NOT an error, so you can retry it as often as you like.\n\nExpect 200 with attached:false and already_had_evidence:true.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": \"{{dryRunVisitId}}\",\n  \"run_id\": \"{{evidenceRunId}}\",\n  \"screenshot\": \"iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const j = pm.response.json();",
                  "pm.test('200 OK', () => pm.response.to.have.status(200));",
                  "pm.test('refused as a no-op, not an error', () => {",
                  "  pm.expect(j.attached, 'attached').to.eql(false);",
                  "  pm.expect(j.already_had_evidence, 'already_had_evidence').to.eql(true);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Evidence 4 - unknown run_id -> 404",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-evidence",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-evidence"
              ]
            },
            "description": "Evidence for a (visit_id, run_id) we have never seen is a 404, never a stored orphan. Post the result FIRST, then attach.\n\nExpect 404 result_not_found_for_visit_run.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"visit_id\": \"{{dryRunVisitId}}\",\n  \"run_id\": \"no-such-run-id\",\n  \"screenshot\": \"iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('404', () => pm.response.to.have.status(404));",
                  "pm.test('names the reason', () => pm.expect(pm.response.json().error).to.eql('result_not_found_for_visit_run'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "5. Monthly LWT report (read-only, no side effects)",
      "item": [
        {
          "name": "Monthly - filing set (START HERE)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly?month={{reportMonth}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly"
              ],
              "query": [
                {
                  "key": "month",
                  "value": "{{reportMonth}}"
                }
              ]
            },
            "description": "The Miami-Dade LWT monthly report for the last completed month.\n\nREAD ONLY: no lease, no dispense, no side effects. Safe to run as often as you like, unlike the queue.\n\nReturns only IN-SCOPE activities. Scope is evaluated PER ACTIVITY, not per ticket: pickup county = Dade OR the ticket offloaded in Miami-Dade. A ticket with no in-scope activity is omitted entirely, and excluded_rows tells you how many rows that cost so nothing is dropped silently.\n\ngallons (changed 2026-09-18, Yan): null on every row of a ticket offloaded in Miami-Dade (white), whose quantity you resolve from the county invoice by decal; on a ticket offloaded outside Miami-Dade (yellow, Broward) it is the grease trap CAPACITY per client (the Jobber Grease Trap Size on the visit property, else the client Pumping service config size), a capacity, not a measured volume, with gallons_source saying which. Each yellow ticket also carries dade_pickup_gallons {total, rows, rows_missing, complete}: SUM the Dade-pickup rows, never take a single row value, never fall back to the Dade decal constant, and do not file a ticket whose complete is false. It is NOT resolved from the decal or from TRUCK capacity, and nothing on the form is computed from either. truck and truck_capacity_gallons are provided. README section 4c."
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Use the last COMPLETED month, which is what a monthly filing is about.",
                  "const d = new Date();",
                  "d.setUTCDate(1); d.setUTCMonth(d.getUTCMonth() - 1);",
                  "const m = d.getUTCFullYear() + '-' + String(d.getUTCMonth() + 1).padStart(2, '0');",
                  "pm.collectionVariables.set('reportMonth', m);",
                  "console.log('reportMonth = ' + m);"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const j = pm.response.json();",
                  "pm.test('200 OK', () => pm.response.to.have.status(200));",
                  "pm.test('shaped as expected', () => {",
                  "  pm.expect(j.month).to.eql(pm.collectionVariables.get('reportMonth'));",
                  "  pm.expect(j.county).to.eql('Miami-Dade');",
                  "  pm.expect(j.tickets).to.be.an('array');",
                  "});",
                  "const rows = (j.tickets || []).flatMap(t => t.rows || []);",
                  "pm.test('every returned row is in scope', () => rows.forEach(r => pm.expect(r.in_scope).to.eql(true)));",
                  "// --- 2026-09-18: gallons on Broward-disposed tickets (Yan; migration 2026-09-18_1455) ------",
                  "// The old assertion here was 'gallons is always null'. The contract is now an INVARIANT set,",
                  "// not a literal count: counts move with every Client App edit of a Grease Trap Size.",
                  "const _tk = (j.tickets || []);",
                  "const _rowsK = _tk.flatMap(t => (t.rows || []).map(r => ({ ...r, _off: t.offload_in_dade })));",
                  "pm.test('gallons: null on every row of a Miami-Dade-offload (white) ticket', () => {",
                  "    // 🛑 THE LOAD-BEARING HALF. The consumer takes ANY non-null row gallons over the county",
                  "    // invoice, so a value on a white row silently changes the Dade filing.",
                  "    const bad = _rowsK.filter(r => r._off === true && (r.gallons !== null || r.gallons_source !== null));",
                  "    pm.expect(bad.map(r => r.client_code + '=' + r.gallons), 'a white row carries gallons').to.eql([]);",
                  "    // anchor: a white row must exist, or the assertion above is vacuous",
                  "    pm.expect(_rowsK.some(r => r._off === true), 'no white row in this month, the check proves nothing').to.eql(true);",
                  "});",
                  "pm.test('gallons and gallons_source are null together, and a value is a positive integer with a known source', () => {",
                  "    _rowsK.forEach(r => pm.expect('gallons_source' in r, 'gallons_source key missing on ' + r.client_code).to.eql(true));",
                  "    const mis = _rowsK.filter(r => (r.gallons === null) !== (r.gallons_source === null));",
                  "    pm.expect(mis.map(r => r.client_code), 'gallons and gallons_source disagree on null').to.eql([]);",
                  "    const badVal = _rowsK.filter(r => r.gallons !== null && !(Number.isInteger(r.gallons) && r.gallons > 0",
                  "        && ['grease_trap_size', 'service_config_size'].includes(r.gallons_source)));",
                  "    pm.expect(badVal.map(r => r.client_code + '=' + JSON.stringify([r.gallons, r.gallons_source])), 'bad gallons value').to.eql([]);",
                  "});",
                  "pm.test('dade_pickup_gallons: null on white tickets, a consistent block on yellow tickets', () => {",
                  "    _tk.forEach(t => {",
                  "        if (t.offload_in_dade === true) { pm.expect(t.dade_pickup_gallons, 'white ticket ' + t.ticket_number).to.eql(null); return; }",
                  "        const g = t.dade_pickup_gallons;",
                  "        pm.expect(g, 'yellow ticket ' + t.ticket_number + ' has no dade_pickup_gallons').to.be.an('object');",
                  "        const dade = (t.rows || []).filter(r => r.pickup_in_dade === true);",
                  "        const missing = dade.filter(r => r.gallons === null).length;",
                  "        const total = dade.reduce((a, r) => a + (r.gallons === null ? 0 : r.gallons), 0);",
                  "        pm.expect([g.total, g.rows, g.rows_missing, g.complete], 'dade_pickup_gallons disagrees with the rows on ' + t.ticket_number)",
                  "            .to.eql([total, dade.length, missing, dade.length > 0 && missing === 0]);",
                  "    });",
                  "});",
                  "// ⚠ SAY WHAT WAS CHECKED: a month with no yellow ticket (2026-09 today) exercises none of the",
                  "// yellow-side assertions. The pinned positive control lives in the next request.",
                  "console.log('yellow tickets this month: ' + _tk.filter(t => t.offload_in_dade === false).length +",
                  "    ', rows with gallons: ' + _rowsK.filter(r => r.gallons !== null).length +",
                  "    (_tk.some(t => t.offload_in_dade === false) ? '' : ' -- NO YELLOW TICKET, the gallons invariants were only exercised on white rows'));",
                  "pm.test(\"pickup_date is the VISIT date, not the dump date\", () => {",
                  "    // 🛑 THE REGRESSION EVERY DOC NAMES AS THE ONE TO WATCH. Until now this asserted only",
                  "    // typeof pickup_date === \"string\", so setting pickup_date := offload_date on every row",
                  "    // left the entire suite GREEN.",
                  "    //",
                  "    // The discriminator is measured, not guessed. derm_manifests.service_date is a misnomer",
                  "    // holding the DUMP date and equals dump_ticket_date on 94.5% of live manifests, so if",
                  "    // pickup_date ever served it, almost every row would EQUAL its offload_date. Live today:",
                  "    // 92% are STRICTLY BEFORE (grease is pumped before it is dumped). No overlap.",
                  "    const pd = pm.response.json().tickets.flatMap(t =>",
                  "        t.rows.map(r => ({ p: r.pickup_date, o: t.offload_date, code: r.client_code })));",
                  "    pd.forEach(r => pm.expect(r.p, \"pickup_date missing on \" + r.code).to.be.a(\"string\"));",
                  "    if (pd.length >= 10) {",
                  "        const nBefore = pd.filter(r => r.p < r.o).length;",
                  "        pm.expect(nBefore * 2 > pd.length,",
                  "            \"only \" + nBefore + \" of \" + pd.length + \" pickups precede their offload -- \" +",
                  "            \"pickup_date looks like it is serving the DUMP date rather than visits.visit_date\"",
                  "        ).to.eql(true);",
                  "    }",
                  "});",
                  "if (pm.response.headers.get('ETag')) { pm.collectionVariables.set('monthlyEtag', pm.response.headers.get('ETag')); }",
                  "console.log(j.ticket_count + ' tickets, ' + j.row_count + ' rows, ' + j.excluded_rows + ' excluded');",
                  "",
                  "// --- 2026-08-25: state + client_name contract ---------------------------------",
                  "// Added because the harness was silent on both fields on the day their shape changed.",
                  "const _rows = pm.response.json().tickets.flatMap(t => t.rows);",
                  "",
                  "pm.test(\"state is a USPS two-letter code on every row\", () => {",
                  "    // A value that is NOT two letters is us passing an UNRECOGNISED state through",
                  "    // verbatim, on purpose, so it is visible rather than silently relabelled to FL.",
                  "    // If this fails, do NOT print that value on the county form -- report it.",
                  "    const bad = _rows.filter(r => r.state !== null && !/^[A-Z]{2}$/.test(r.state));",
                  "    pm.expect(bad.map(r => r.client_code + \"=\" + JSON.stringify(r.state)), ",
                  "              \"unrecognised state passed through\").to.eql([]);",
                  "",
                  "    // 🛑 AND FL MUST ACTUALLY BE PRESENT. The filter above skips nulls BEFORE testing shape,",
                  "    // and nothing else asserts a state is ever there -- so setting state := null on all 80",
                  "    // rows of a live month passed this entire suite. That is the State column printing blank",
                  "    // on a whole Miami-Dade filing.",
                  "    // 🛑 A CROSS-FIELD CHECK, because a presence check cannot see a wrong-but-valid state.",
                  "    //    `some(r => r.state === \"FL\")` passes if ANY single row is FL, so FL->\"GL\" on one row,",
                  "    //    or ->NY on half of them, or on 79 of 80, all passed on every month. The shape check",
                  "    //    accepts NY, GL and FK alike -- being two capitals is not being RIGHT.",
                  "    //    County and state are independent fields from the same property, so they must agree:",
                  "    //    every county we serve is a Florida county (live: Dade 559, Broward 120, Palm Beach 7,",
                  "    //    plus 14 no-property rows).",
                  "    //    ⚠ The obvious justification -- \"a non-Florida property carries a non-Florida county\"",
                  "    //    -- is NOT true of our data, and the real reason is stronger. Measured: properties",
                  "    //    154, 396 and 567 are Beverly Hills addresses carrying state=California AND",
                  "    //    county=Dade. Since the view derives in_scope from county='Dade', one of those taking",
                  "    //    a pickup would be FILED AS MIAMI-DADE ACTIVITY on the strength of a wrong county.",
                  "    //    So this check going red on them is a TRUE positive, not a false alarm -- it is the",
                  "    //    only thing that would notice. (All three have zero visits today, so it is latent.)",
                  "    //    The genuinely out-of-state rows -- Manhattan Beach, Cedarhurst, the two Levis",
                  "    //    addresses -- carry county=NULL and are not matched by FL_COUNTIES at all.",
                  "    const FL_COUNTIES = [\"Dade\", \"Broward\", \"Palm Beach\", \"Monroe\", \"Collier\", \"Lee\",",
                  "                         \"Martin\", \"St. Lucie\", \"Indian River\", \"Okeechobee\", \"Hendry\", \"Glades\"];",
                  "    const mismatch = _rows.filter(r => FL_COUNTIES.includes(r.county) && r.state !== \"FL\");",
                  "    pm.expect(mismatch.map(r => r.client_code + \": county \" + r.county + \" but state \" +",
                  "                                JSON.stringify(r.state)),",
                  "        \"a Florida county is paired with a non-Florida state -- one of the two is wrong, and \" +",
                  "        \"both print on the county form\").to.eql([]);",
                  "",
                  "    pm.expect(_rows.some(r => r.state === \"FL\"),",
                  "        \"no row carries a state at all. If the addresses are gone too this is a broken \" +",
                  "        \"property join; if the addresses are intact the STATE MAPPING itself has failed. \" +",
                  "        \"Either way the State column would print blank on the whole filing\").to.eql(true);",
                  "",
                  "    // 🛑 AND A NULL STATE MUST MEAN \"NO PROPERTY AT ALL\". Measured on the live view: the null",
                  "    // crosstab is an EXACT DIAGONAL (measured 2026-08-25; the counts grow, the SHAPE is the invariant) -- 686 rows with none of state/address/city/zip/county",
                  "    // null, and 14 with ALL FIVE null. No third combination exists. So a null state beside a",
                  "    // present address is a NEW defect, not the known no-property case.",
                  "    const halfNull = _rows.filter(r => r.state === null && (r.address !== null || r.city !== null));",
                  "    pm.expect(halfNull.map(r => r.client_code),",
                  "        \"state went missing on its own, with the address still present\").to.eql([]);",
                  "",
                  "    // 🛑 THE DIAGONAL IS AN \"IFF\", AND THE GUARD ABOVE ONLY TESTED ONE DIRECTION.",
                  "    // Nulling address on every row was caught on 7 of 8 months ONLY by the accent test --",
                  "    // whose message then blamed the accent fold -- and on 2026-03, which carries no accent",
                  "    // carrier at all, NOTHING failed. Live the crosstab is exactly two combinations:",
                  "    // 686 rows with none of state/address/city/zip/county null, and 14 with ALL FIVE null.",
                  "    // Blank-string is a different shape and is caught by the emptiness check below.",
                  "    const loc = r => [r.state, r.address, r.city, r.zip, r.county];",
                  "    const mixed = _rows.filter(r => { const n = loc(r).filter(v => v === null).length;",
                  "                                      return n !== 0 && n !== 5; });",
                  "    pm.expect(mixed.map(r => r.client_code + \"=\" + JSON.stringify(loc(r))),",
                  "        \"a row has SOME location fields null and others populated -- the live shape is all-or-nothing, so this is a new defect, not the known no-property case\").to.eql([]);",
                  "    // ⚠ zip and state were MISSING from this list when it was written, so a blank ZIP on every",
                  "    //   row of a filing passed all 22 tests on all 8 months -- exactly what the message below",
                  "    //   says it prevents. Enumerate every location field, not the ones that came to mind.",
                  "    const blankLoc = _rows.filter(r => [r.address, r.city, r.county, r.zip, r.state].some(",
                  "        v => typeof v === \"string\" && !v.trim()));",
                  "    pm.expect(blankLoc.map(r => r.client_code),",
                  "        \"a location field is an empty string -- it would print blank on the form\").to.eql([]);",
                  "",
                  "    // zip must look like a zip when present. Live: 0 of 700 malformed.",
                  "    const oddZip = _rows.filter(r => r.zip !== null && !/^\\d{5}(-\\d{4})?$/.test(String(r.zip)));",
                  "    pm.expect(oddZip.map(r => r.client_code + \"=\" + JSON.stringify(r.zip)),",
                  "        \"a zip is not a 5-digit or ZIP+4 value\").to.eql([]);",
                  "});",
                  "",
                  "pm.test(\"state is never the long form\", () => {",
                  "    pm.expect(_rows.filter(r => r.state === \"Florida\"), \"pre-2026-08-25 long form\").to.eql([]);",
                  "});",
                  "",
                  "pm.test(\"client_name carries no typographic punctuation\", () => {",
                  "    // curly quotes, en/em dash, NBSP. Folded to ASCII at the source.",
                  "    const junk = /[\\u2018\\u2019\\u201C\\u201D\\u2013\\u2014\\u00A0]/;",
                  "    const bad = _rows.filter(r => junk.test(r.client_name || \"\"));",
                  "    pm.expect(bad.map(r => r.client_code), \"typographic punctuation\").to.eql([]);",
                  "});",
                  "",
                  "pm.test(\"accented LETTERS are preserved, not stripped\", () => {",
                  "    // Espanola Way and Fendi Chateau are CORRECT spellings of a real street and a real",
                  "    // business. A run with zero accents where accents are expected is a REGRESSION, not a",
                  "    // cleaner result -- it means somebody \"fixed\" the fold into an unaccent().",
                  "    //",
                  "    // The guard names the three codes that ACTUALLY carry an accent, measured against the",
                  "    // live view: 014-JOY and 179-CIG (448/409 Espanola Way, in address) and 167-FEN (Fendi",
                  "    // Chateau, in name). An earlier version named 016-FIA/017-FIA -- those are Fialkoff's,",
                  "    // whose only non-ASCII was the curly apostrophe THIS FOLD REMOVES, so the guard could",
                  "    // never fire on them and 2026-05 was silently skipped.",
                  "    const accented = /[\\u00C0-\\u024F]/;",
                  "    const carriers = _rows.filter(r => /014-JOY|167-FEN|179-CIG/.test(r.client_code));",
                  "    // 🛑 SAY HOW MANY WERE CHECKED. The body below sits inside `if (carriers.length)`, so a",
                  "    // green tick meant EITHER \"three carriers verified\" OR \"nothing was checked at all\" --",
                  "    // and 2026-03 genuinely contains no carrier row, so the silent case is real, not",
                  "    // hypothetical. Renaming the three codes would have left the suite green having asserted",
                  "    // nothing.",
                  "    console.log(\"accent carriers checked: \" + carriers.length +",
                  "        (carriers.length ? \" (\" + carriers.map(r => r.client_code).join(\",\") + \")\"",
                  "                         : \" -- NOTHING WAS VERIFIED THIS MONTH\"));",
                  "    if (carriers.length) {",
                  "        // Exact strings, not \"contains some accent\": a fold that stripped ONE of the two",
                  "        // accented characters would still satisfy a contains-check.",
                  "        const EXPECT = { \"167-FEN\": \"Fendi Ch\\u00e2teau Residences\" };",
                  "        carriers.forEach(r => {",
                  "            if (EXPECT[r.client_code]) {",
                  "                pm.expect(r.client_name, \"exact name for \" + r.client_code)",
                  "                    .to.eql(EXPECT[r.client_code]);",
                  "            }",
                  "            if (/014-JOY|179-CIG/.test(r.client_code)) {",
                  "                pm.expect(/Espa\\u00f1ola Way/.test(r.address || \"\"),",
                  "                    \"n-tilde stripped from \" + r.client_code + \": \" + r.address).to.eql(true);",
                  "            }",
                  "        });",
                  "        const kept = carriers.filter(r => accented.test((r.client_name||\"\") + (r.address||\"\")));",
                  "        pm.expect(kept.length, \"accents were stripped from \" +",
                  "            carriers.map(r=>r.client_code).join(\",\")).to.eql(carriers.length);",
                  "    }",
                  "});",
                  "",
                  "pm.test(\"rows are returned in the documented total order\", () => {",
                  "    // offload_date, ticket_number, pickup_date, visit_id, manifest_id.",
                  "    // 🛑 THIS MUST COMPARE SEQUENCE, NOT UNIQUENESS. An earlier version asserted that",
                  "    // visit_id was unique per row -- true on all 690 rows and true with every .order()",
                  "    // removed, so it tested nothing. Before 2026-08-25 the sort had 3 keys and 571 of 690",
                  "    // rows sat in tie groups, so identical calls could differ in sequence.",
                  "    const j = pm.response.json();",
                  "    const key = t => [t.offload_date, t.ticket_number];",
                  "    const tk = j.tickets.map(key);",
                  "    for (let i = 1; i < tk.length; i++) {",
                  "        pm.expect(JSON.stringify(tk[i-1]) <= JSON.stringify(tk[i]),",
                  "            \"tickets out of order at \" + i + \": \" + tk[i-1] + \" then \" + tk[i]).to.eql(true);",
                  "    }",
                  "    j.tickets.forEach(t => {",
                  "        const rk = t.rows.map(r => [r.pickup_date, String(r.visit_id).padStart(10,\"0\")].join(\"|\"));",
                  "        for (let i = 1; i < rk.length; i++) {",
                  "            pm.expect(rk[i-1] <= rk[i], \"rows out of order in ticket \" + t.ticket_number +",
                  "                \" at \" + i + \": \" + rk[i-1] + \" then \" + rk[i]).to.eql(true);",
                  "        }",
                  "    });",
                  "});",
                  "",
                  "// Record the filing size so the include=all request can actually compare against it. Its",
                  "// test asserted only row_count >= 1, which cannot fail on any month that returns a row.",
                  "pm.collectionVariables.set(\"monthlyFilingRowCount\", String(pm.response.json().row_count));",
                  "pm.collectionVariables.set(\"monthlyFilingExcluded\", String(pm.response.json().excluded_rows));",
                  "",
                  "// --- 2026-08-25 iteration 8: three fields nothing was asserting -----------------",
                  "",
                  "pm.test(\"client_name is present on every row\", () => {",
                  "    // 🛑 Nothing asserted this. Blanking the Business Name column on the whole filing passed",
                  "    // all ten tests on 6 of 8 months: the punctuation test reads `r.client_name || \"\"`, so a",
                  "    // blank is vacuously clean, and the only other reference compares exact names for the",
                  "    // three ACCENT CARRIERS. On the two months it did fail, the message blamed the accent",
                  "    // fold for a blank-name defect -- a misleading failure is barely better than none.",
                  "    // This is the same hole closed for `state`, left open on the sibling field.",
                  "    // Live: 0 of 700 view rows are null or blank, so there is no false-positive risk.",
                  "    const blank = _rows.filter(r => !r.client_name || !String(r.client_name).trim());",
                  "    pm.expect(blank.map(r => r.client_code || \"(no code)\"),",
                  "        \"client_name blank -- the Business Name column would print empty\").to.eql([]);",
                  "});",
                  "",
                  "pm.test(\"data_quality.checked is true\", () => {",
                  "    // 🛑 The folder contained ZERO references to data_quality, checked, conflicts or anomaly,",
                  "    // so `checked: false` with conflicts emptied passed every test on every month.",
                  "    // postman/README.md 4c is explicit that this is the one thing to read before filing:",
                  "    // checked:false means the CHECK failed, so an empty conflicts list proves nothing.",
                  "    // Same fail-open family as the null-state blind spot. Live: true on all 8 months.",
                  "    const dq = pm.response.json().data_quality;",
                  "    pm.expect(dq, \"data_quality block missing entirely\").to.not.eql(undefined);",
                  "    pm.expect(dq.checked,",
                  "        \"data_quality.checked is not true -- the conflict overlay FAILED, so an empty \" +",
                  "        \"conflicts list proves nothing. Do NOT file on this payload\").to.eql(true);",
                  "});",
                  "",
                  "pm.test(\"truck_capacity_gallons is a known fleet capacity\", () => {",
                  "    // 🛑 CORRECTED 2026-08-26: this is NOT the filed quantity and NOT the fee basis. Jonathan",
                  "    // confirmed the county figure is per manifest off the invoice (a per-decal constant, corrected 2026-09-18: not a measurement), and ticket 828837",
                  "    // is Moises / decal C1184 / capacity 9000 here while the county bills it at 3,800. This test",
                  "    // is still worth having as an INTERNAL CONSISTENCY check (a truck must report its own",
                  "    // capacity), but nothing on the county form is computed from it. Do not restore the old",
                  "    // wording: it would send the next reader to the wrong field. (`gallons` was null on 700/700 until 2026-09-18; it now carries a trap capacity on Broward-disposed tickets only, see the gallons tests above.)",
                  "    // Measured live 2026-09-18: Moises 3840 (was 9000 in this file until then; vehicles.grease_tank_capacity_gallons reads 3840), David 1800, Cloggy 126 (Goliath 4800 is decommissioned but",
                  "    // still appears on historical rows). 6 of 700 rows carry no truck, and those correctly",
                  "    // carry no capacity -- that is a known gap, not a failure, so it is allowed through.",
                  "    // 0 rows have a truck with a null capacity, and 0 carry a capacity outside the fleet.",
                  "    // 🛑 PER-TRUCK, not membership. A membership check let every David row report 9000 with",
                  "    // NOTHING FAILING on any month. Still a real defect (the value is wrong), but the consequence",
                  "    // is INTERNAL, not a five-fold county fee. The mapping is strictly 1:1",
                  "    // live (Moises 3840, David 1800, Cloggy 126; 0 trucks carry two",
                  "    // capacities), so naming it costs nothing in false positives.",
                  "    // Goliath 4800 is decommissioned but may still appear on historical rows.",
                  "    const CAP = { \"Moises\": 3840, \"David\": 1800, \"Cloggy\": 126, \"Goliath\": 4800 };",
                  "    const FLEET = [3840, 4800, 1800, 126];",
                  "    const withTruck = _rows.filter(r => r.truck !== null && r.truck !== undefined);",
                  "    const badCap = withTruck.filter(r => !FLEET.includes(r.truck_capacity_gallons));",
                  "    const wrongCap = withTruck.filter(r => CAP[r.truck] !== undefined &&",
                  "                                           r.truck_capacity_gallons !== CAP[r.truck]);",
                  "    pm.expect(wrongCap.map(r => r.truck + \" reported \" + r.truck_capacity_gallons +",
                  "                                \" but is \" + CAP[r.truck]),",
                  "        \"a truck is reporting the WRONG capacity for itself, an internal consistency error. \" +",
                  "        \"NOTE: this is NOT the filed quantity; the Dade figure is the per-decal invoice constant, the Broward figure is the row gallons\").to.eql([]);",
                  "    pm.expect(badCap.map(r => r.truck + \"=\" + JSON.stringify(r.truck_capacity_gallons)),",
                  "        \"a truck carries a capacity that is not one of ours, an internal consistency \" +",
                  "        \"error, not a filed-quantity error\").to.eql([]);",
                  "    // and a row with no truck must not invent a capacity",
                  "    const ghost = _rows.filter(r => (r.truck === null || r.truck === undefined) &&",
                  "                                     r.truck_capacity_gallons !== null);",
                  "    pm.expect(ghost.length, \"a row with no truck carries a capacity\").to.eql(0);",
                  "});",
                  "",
                  "pm.test(\"ticket_number is present and distinct per ticket\", () => {",
                  "    // 🛑 The manifest identifier PRINTED ON THE COUNTY FORM, and nothing asserted it.",
                  "    // null / \"\" / deleted / a uniform \"999999\" across every ticket all passed every month --",
                  "    // even the ordering test, whose sort key stays monotonic under a uniform value.",
                  "    // Live: 0 null and 0 blank of 700 rows, and every ticket object carries its own number.",
                  "    const t = pm.response.json().tickets;",
                  "    const bad = t.filter(x => !x.ticket_number || !String(x.ticket_number).trim());",
                  "    pm.expect(bad.length, \"a ticket has no ticket_number -- the Manifest/Ticket # column \" +",
                  "        \"would print blank on the filing\").to.eql(0);",
                  "    const nums = t.map(x => x.ticket_number);",
                  "    pm.expect(new Set(nums).size,",
                  "        \"two tickets share a ticket_number -- a uniform or duplicated identifier makes the \" +",
                  "        \"filing unreconcilable against the paper manifests\").to.eql(nums.length);",
                  "});",
                  "",
                  "pm.test(\"the conflicts payload is internally consistent\", () => {",
                  "    // 🛑 Only data_quality.checked was asserted; the payload it guards was not. Emptying",
                  "    // conflicts, flipping violates_guard to false, or stamping every row with an anomaly all",
                  "    // survived on every month -- including 2026-07, which carries the live visit 6756 /",
                  "    // ticket 830673 conflict that README 4c says to check against the paper before filing.",
                  "    // Product-reachable: index.ts filters conflicts to visit ids shown this month, so a",
                  "    // join-key regression yields checked:true / count:0 / conflicts:[] -- indistinguishable",
                  "    // from a genuinely clean month unless the pieces are cross-checked.",
                  "    const j = pm.response.json();",
                  "    const dq = j.data_quality;",
                  "    pm.expect(dq.conflicts.length, \"conflict_count disagrees with the conflicts array\")",
                  "        .to.eql(dq.conflict_count);",
                  "    dq.conflicts.forEach(cf => {",
                  "    // ⚠ `x === undefined || x === null` PASSES A BLANK STRING, so conflict_kind := \"\" survived.",
                  "    //   Check emptiness, not just presence.",
                  "        [\"visit_id\", \"ticket_number\", \"conflict_kind\"].forEach(k =>",
                  "            pm.expect(cf[k] === undefined || cf[k] === null,",
                  "                \"conflict is missing \" + k).to.eql(false));",
                  "        pm.expect(String(cf.conflict_kind || \"\").trim() === \"\",",
                  "            \"conflict_kind is blank -- the reason a row was flagged is the whole point of \" +",
                  "            \"reporting it\").to.eql(false);",
                  "        pm.expect(j.tickets.some(t => t.ticket_number === cf.ticket_number),",
                  "            \"a conflict names ticket \" + cf.ticket_number + \", which is not in this month's \" +",
                  "            \"response -- the conflict and the filing disagree about what was returned\")",
                  "            .to.eql(true);",
                  "    });",
                  "    // every flagged ROW must correspond to a reported conflict, and vice versa",
                  "    const rowFlagged = j.tickets.flatMap(t => t.rows).filter(r => r.anomaly).map(r => r.visit_id).sort();",
                  "    const reported = dq.conflicts.map(cf => cf.visit_id).sort();",
                  "    pm.expect(rowFlagged, \"the rows carrying an anomaly do not match data_quality.conflicts -- \" +",
                  "        \"one of the two is lying about what was found\").to.eql(reported);",
                  "});",
                  "",
                  "pm.test(\"each row is a distinct activity\", () => {",
                  "    // 🛑 A join fan-out that duplicates every filed row DOUBLES the reported activities and,",
                  "    // a duplicated manifest line would double its billed gallons too. It passed every test on the",
                  "    // four months carrying no conflict, and on the other four failed only with a message",
                  "    // blaming the conflicts payload.",
                  "    // ⚠ An earlier version of the ordering test asserted visit_id UNIQUENESS and was replaced",
                  "    // by a SEQUENCE assertion because uniqueness alone tested nothing about order. The right",
                  "    // answer was BOTH, not either. Live: 700 distinct visit_ids across 700 rows.",
                  "    const ids = _rows.map(r => r.visit_id);",
                  "    const dupes = ids.filter((v, i) => ids.indexOf(v) !== i);",
                  "    pm.expect([...new Set(dupes)],",
                  "        \"the same visit appears on more than one row -- a join fan-out inflates both the \" +",
                  "        \"activity count and would duplicate a manifest line\").to.eql([]);",
                  "});",
                  "",
                  "pm.test(\"the response accounting adds up\", () => {",
                  "    // ticket_count and row_count are what a reader trusts without counting. Live they are",
                  "    // exact; nothing asserted them, so ticket_count := 999 survived every month.",
                  "    const j = pm.response.json();",
                  "    pm.expect(j.tickets.length, \"ticket_count disagrees with the tickets array\")",
                  "        .to.eql(j.ticket_count);",
                  "    const rows = j.tickets.reduce((n, t) => n + t.rows.length, 0);",
                  "    pm.expect(rows, \"row_count disagrees with the rows actually returned\").to.eql(j.row_count);",
                  "    const empty = j.tickets.filter(t => t.rows.length === 0);",
                  "    pm.expect(empty.map(t => t.ticket_number),",
                  "        \"a ticket carries zero rows -- README 4c says a ticket with no in-scope activity is \" +",
                  "        \"omitted entirely, so an empty one should not appear\").to.eql([]);",
                  "});",
                  "",
                  "pm.test(\"truck_decal is present, and is the RIGHT decal for the truck\", () => {",
                  "    // 🛑 This is the permit number that reaches the county. A wrong decal files under",
                  "    // a different vehicle; an invented one puts a permit number we do not hold on a filing.",
                  "    // Live mapping is strictly 1:1 and Miami-Dade only: Moises C1184 (425 rows),",
                  "    // David C0976 (224). Cloggy holds NO decal in any jurisdiction and 6 rows carry no truck,",
                  "    // so 51 of 700 rows are legitimately null and the caller must REFUSE those tickets.",
                  "    const DECAL = { \"Moises\": \"C1184\", \"David\": \"C0976\" };",
                  "    const rows = _rows;",
                  "    pm.expect(rows.every(r => \"truck_decal\" in r),",
                  "        \"truck_decal is missing from the row shape entirely -- the caller has no permit number\").to.eql(true);",
                  "    const wrong = rows.filter(r => DECAL[r.truck] !== undefined && r.truck_decal !== DECAL[r.truck]);",
                  "    pm.expect(wrong.map(r => r.truck + \" reported \" + JSON.stringify(r.truck_decal) +",
                  "                             \" but holds \" + DECAL[r.truck]),",
                  "        \"a truck is reporting the WRONG decal -- this is the permit number that reaches the county\").to.eql([]);",
                  "    // a truck we hold no decal for must be null, never guessed at",
                  "    const invented = rows.filter(r => DECAL[r.truck] === undefined && r.truck_decal !== null);",
                  "    pm.expect(invented.map(r => JSON.stringify(r.truck) + \"=\" + JSON.stringify(r.truck_decal)),",
                  "        \"a row with no known decal carries one anyway -- a guessed permit number\").to.eql([]);",
                  "    // and a row with no truck cannot carry a decal",
                  "    const ghost = rows.filter(r => (r.truck === null || r.truck === undefined) && r.truck_decal !== null);",
                  "    pm.expect(ghost.length, \"a row with no truck carries a decal\").to.eql(0);",
                  "});",
                  "",
                  "pm.test(\"truck_decals reconciles with TRUCKS, and is NOT index-aligned with it\", () => {",
                  "    // 🛑 GRAIN. `trucks` and `truck_decals` are built from EVERY row on the manifest, but",
                  "    // `rows` is filtered to in-scope rows unless you pass include=all. So truck_decals can",
                  "    // legitimately name a decal that appears on NO row you were served, and asserting",
                  "    // set-EQUALITY against the served rows is comparing a manifest-grained field to a",
                  "    // filing-grained one. The first version of this test did exactly that and failed on",
                  "    // ticket 312024 (Aug): David ran two Broward pickups on that manifest, both out of",
                  "    // scope, so every served row is Moises/C1184 while truck_decals is [\"C0976\",\"C1184\"].",
                  "    // 1 of 118 live tickets shows it, which is precisely the density that survives a",
                  "    // spot-check and breaks a filing.",
                  "    // ⚠ AND the two arrays de-duplicate INDEPENDENTLY: a truck holding no decal (Cloggy)",
                  "    // contributes to `trucks` and not to `truck_decals`, so they differ in length. A caller",
                  "    // zipping them by index would attribute Cloggy loads to David.",
                  "    // ⇒ FILE FROM THE ROW'S OWN truck_decal. truck_decals is a manifest summary, not the",
                  "    // set of decals belonging on your filing.",
                  "    const DECAL_T = { \"Moises\": \"C1184\", \"David\": \"C0976\" };",
                  "    const t = pm.response.json().tickets;",
                  "    t.forEach(tk => {",
                  "        pm.expect(Array.isArray(tk.truck_decals),",
                  "            \"ticket \" + tk.ticket_number + \" has no truck_decals array\").to.eql(true);",
                  "        // (1) SAME GRAIN: truck_decals is exactly the decals of the trucks on the manifest.",
                  "        const fromTrucks = [...new Set((tk.trucks || []).map(x => DECAL_T[x]).filter(Boolean))].sort();",
                  "        pm.expect([...tk.truck_decals].sort(),",
                  "            \"ticket \" + tk.ticket_number + \": truck_decals does not reconcile with trucks\").to.eql(fromTrucks);",
                  "        // (2) Subset, never equality: a decal on a row we DID serve must appear in the set.",
                  "        const rowDecals = [...new Set(tk.rows.map(r => r.truck_decal).filter(Boolean))];",
                  "        pm.expect(rowDecals.filter(d => !tk.truck_decals.includes(d)),",
                  "        // No length check here on purpose: (1) pins truck_decals to the deduped decals of",
                  "        // trucks, so it can never be the longer array. An assertion that cannot fail is not one.",
                  "            \"ticket \" + tk.ticket_number + \": a served row carries a decal missing from truck_decals\").to.eql([]);",
                  "    });",
                  "    // Not a failure, but the caller needs to SEE it rather than discover it on a filed form.",
                  "    const trap = t.filter(tk => tk.truck_decals.some(d => !tk.rows.some(r => r.truck_decal === d)));",
                  "    if (trap.length) console.log(\"NOTE: \" + trap.length + \" ticket(s) carry a decal used by no \" +",
                  "        \"served row, because the truck's work on that manifest was out of scope: \" +",
                  "        trap.map(x => x.ticket_number).join(\", \") + \" -- file from the row's own truck_decal.\");",
                  "});",
                  "",
                  "pm.test(\"every ticket carries filing state, and filing agrees with reported\", () => {",
                  "    // 🛑 The two fields must never disagree: reported=true with filing=null would tell staff a",
                  "    // month was filed while showing nothing to point at, and the reverse would hide a real filing.",
                  "    // ⚠ confirmation_ref is routinely NULL and that is NOT a failure -- the county returns no",
                  "    // confirmation number for the monthly form. Never infer a problem from its absence.",
                  "    const t = pm.response.json().tickets;",
                  "    const noFlag = t.filter(x => typeof x.reported !== \"boolean\");",
                  "    pm.expect(noFlag.map(x => x.ticket_number),",
                  "        \"a ticket has no boolean `reported`\").to.eql([]);",
                  "    const disagree = t.filter(x => x.reported === (x.filing === null));",
                  "    pm.expect(disagree.map(x => x.ticket_number + \" reported=\" + x.reported + \" filing=\" + JSON.stringify(x.filing)),",
                  "        \"reported and filing disagree -- one of the two is lying about whether it was filed\").to.eql([]);",
                  "    t.filter(x => x.reported).forEach(x => {",
                  "        [\"invoice_id\",\"period_start\",\"period_end\",\"filed_at\",\"confirmation_ref\",\"filed_by_email\"]",
                  "          .forEach(k => pm.expect(k in x.filing,",
                  "            \"ticket \" + x.ticket_number + \" filing block is missing \" + k).to.eql(true));",
                  "        pm.expect(x.filing.period_end >= x.filing.period_start,",
                  "            \"ticket \" + x.ticket_number + \" filing period ends before it starts\").to.eql(true);",
                  "    });",
                  "    // capture a real ticket for folder 6",
                  "    if (t.length) pm.collectionVariables.set(\"lwtTicket\", String(t[0].ticket_number));",
                  "});",
                  "",
                  "pm.test(\"mode is reported, and says month\", () => {",
                  "    pm.expect(pm.response.json().mode,",
                  "        \"mode is missing or wrong -- a caller cannot tell a month pull from an unreported pull\").to.eql(\"month\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Monthly - include=all (the superset you filter yourself)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly?month={{reportMonth}}&include=all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly"
              ],
              "query": [
                {
                  "key": "month",
                  "value": "{{reportMonth}}"
                },
                {
                  "key": "include",
                  "value": "all"
                }
              ]
            },
            "description": "The same month with NOTHING filtered: every row of every ticket, in-scope or not, each carrying county / pickup_in_dade / in_scope.\n\nUse this if you would rather apply the scope rule yourself. row_count here equals the filing set plus excluded_rows."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const j = pm.response.json();",
                  "pm.test('200 OK', () => pm.response.to.have.status(200));",
                  "pm.test('include flag echoed', () => pm.expect(j.include).to.eql('all'));",
                  "pm.test(\"the superset really is a superset of the filing set\", () => {",
                  "    // Was: pm.expect(j.row_count).to.be.at.least(1) -- unfailable on any month returning a",
                  "    // row, and it never referenced the filing set at all. The filing request now stores its",
                  "    // own row_count so this can compare, and the arithmetic below was never asserted anywhere.",
                  "    const all = pm.response.json().row_count;",
                  "    const filed = Number(pm.collectionVariables.get(\"monthlyFilingRowCount\"));",
                  "    const excl  = Number(pm.collectionVariables.get(\"monthlyFilingExcluded\"));",
                  "    pm.expect(Number.isFinite(filed), \"run the filing request first\").to.eql(true);",
                  "    pm.expect(all >= filed,",
                  "        \"include=all (\" + all + \") is smaller than the filing set (\" + filed + \")\").to.eql(true);",
                  "    pm.expect(filed + excl, \"filed + excluded should equal the superset\").to.eql(all);",
                  "",
                  "    // ⚠ Every guard above degenerates gracefully at equality, so stripping all out-of-scope",
                  "    // rows AND reporting excluded_rows = 0 satisfies the lot (80 >= 80, 80 + 0 === 80).",
                  "    // Scope filtering is the entire purpose of this endpoint, so assert it still happens.",
                  "    // Measured across all 8 months of 2026 (2026-08-25): excluded_rows runs 10..19 and is never 0.",
                  "    if (all >= 20) {",
                  "        pm.expect(excl > 0,",
                  "            \"the filing set excluded nothing from a \" + all + \"-row superset -- either every \" +",
                  "            \"pickup really was in Miami-Dade, or the scope filter has stopped filtering\"",
                  "        ).to.eql(true);",
                  "    }",
                  "});",
                  "const rows = (j.tickets || []).flatMap(t => t.rows || []);",
                  "pm.test('carries out-of-scope rows too', () => pm.expect(rows.some(r => r.in_scope === false) || j.excluded_rows === 0).to.eql(true));"
                ]
              }
            }
          ]
        },
        {
          "name": "Monthly - 2026-08 gallons on Broward tickets (pinned positive control)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly?month=2026-08",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly"
              ],
              "query": [
                {
                  "key": "month",
                  "value": "2026-08"
                }
              ]
            },
            "description": "PINNED to 2026-08 on purpose: the START HERE request follows the wall clock, and a month with no Broward-disposed ticket (2026-09 at the time of writing) exercises none of the gallons invariants, so a broken feed could stay green. August 2026 holds 5 Broward-disposed tickets in default mode. Asserts >= 1, never == N: the counts move every time a Grease Trap Size is typed into the Client App, and moving is the point.\n\nWhat this proves: the yellow rows carry a positive integer trap capacity with gallons_source set, at least one comes from the property arm, every yellow ticket carries dade_pickup_gallons consistent with its rows, and the white tickets of the same month carry nothing. It logs how many August rows still lack a size and which arm served the rest."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const j = pm.response.json();",
                  "pm.test('200 OK', () => pm.response.to.have.status(200));",
                  "const yt = (j.tickets || []).filter(t => t.offload_in_dade === false);",
                  "const wt = (j.tickets || []).filter(t => t.offload_in_dade === true);",
                  "const yrows = yt.flatMap(t => t.rows || []);",
                  "pm.test('2026-08 serves at least one Broward-disposed ticket (else this control proves nothing)', () => pm.expect(yt.length).to.be.at.least(1));",
                  "pm.test('at least one yellow row carries gallons from the property arm', () => {",
                  "    pm.expect(yrows.filter(r => r.gallons_source === 'grease_trap_size').length).to.be.at.least(1);",
                  "});",
                  "pm.test('every yellow gallons value is a positive integer with a known source', () => {",
                  "    const bad = yrows.filter(r => r.gallons !== null && !(Number.isInteger(r.gallons) && r.gallons > 0 && ['grease_trap_size', 'service_config_size'].includes(r.gallons_source)));",
                  "    pm.expect(bad.map(r => r.client_code + '=' + JSON.stringify([r.gallons, r.gallons_source]))).to.eql([]);",
                  "});",
                  "pm.test('white tickets of the same month carry nothing', () => {",
                  "    pm.expect(wt.length, 'no white ticket in 2026-08, the negative half is vacuous').to.be.at.least(1);",
                  "    pm.expect(wt.flatMap(t => t.rows).filter(r => r.gallons !== null).length).to.eql(0);",
                  "    wt.forEach(t => pm.expect(t.dade_pickup_gallons, 'white ' + t.ticket_number).to.eql(null));",
                  "});",
                  "pm.test('dade_pickup_gallons agrees with the rows on every yellow ticket', () => {",
                  "    yt.forEach(t => {",
                  "        const g = t.dade_pickup_gallons; pm.expect(g, t.ticket_number).to.be.an('object');",
                  "        const dade = t.rows.filter(r => r.pickup_in_dade === true);",
                  "        const missing = dade.filter(r => r.gallons === null).length;",
                  "        pm.expect([g.total, g.rows, g.rows_missing, g.complete], t.ticket_number)",
                  "            .to.eql([dade.reduce((a, r) => a + (r.gallons === null ? 0 : r.gallons), 0), dade.length, missing, dade.length > 0 && missing === 0]);",
                  "    });",
                  "});",
                  "const sc = yrows.filter(r => r.gallons_source === 'service_config_size').length;",
                  "const missing = yrows.filter(r => r.gallons === null).map(r => r.client_code);",
                  "console.log('2026-08: ' + yt.length + ' Broward tickets, ' + yrows.length + ' Dade-pickup rows, ' + (yrows.length - missing.length) + ' with gallons (' + sc + ' from the service_config fallback), missing: ' + (missing.join(', ') || 'none') + '; complete tickets: ' + yt.filter(t => t.dade_pickup_gallons.complete).map(t => t.ticket_number).join(', '));"
                ]
              }
            }
          ]
        },
        {
          "name": "Monthly - ETag revalidation -> 304",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly?month={{reportMonth}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly"
              ],
              "query": [
                {
                  "key": "month",
                  "value": "{{reportMonth}}"
                }
              ]
            },
            "description": "Send the ETag from the first request back as If-None-Match. A repeated poll for an unchanged month costs a 304 with no body.\n\nRun \"Monthly - filing set\" first so monthlyEtag is captured."
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.request.headers.add({ key: 'If-None-Match', value: pm.collectionVariables.get('monthlyEtag') || '' });"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('304 Not Modified', () => pm.response.to.have.status(304));"
                ]
              }
            }
          ]
        },
        {
          "name": "Monthly - bad month -> 400",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly?month=2026-13",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly"
              ],
              "query": [
                {
                  "key": "month",
                  "value": "2026-13"
                }
              ]
            },
            "description": "Month must be YYYY-MM with a real month number. Expect 400 month_required_yyyy_mm."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('400', () => pm.response.to.have.status(400));",
                  "pm.test('names the reason', () => pm.expect(pm.response.json().error).to.eql('month_required_yyyy_mm'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Monthly - unreported=1 (selects by FILING STATE, not by month)",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "x-rpa-key",
                "value": "{{rpaBotKey}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly?unreported=1",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly"
              ],
              "query": [
                {
                  "key": "unreported",
                  "value": "1"
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Jonathan, 2026-08-25: \"your month= selects on offload date, but our filing window is the",
                  "// invoice package, and packages don't align to months -- SP00013840 runs 06/28-07/25.",
                  "// 'All tickets not yet marked reported' replaces that guesswork.\"",
                  "pm.test(\"200 OK\", () => pm.response.to.have.status(200));",
                  "pm.test(\"mode says unreported and month is null\", () => {",
                  "    const j = pm.response.json();",
                  "    // 🛑 month MUST be null here. Emitting a month would invite a caller to believe the package",
                  "    // window equals a calendar month, which is the exact assumption this mode exists to remove.",
                  "    pm.expect(j.mode, \"mode is not unreported\").to.eql(\"unreported\");",
                  "    pm.expect(j.month, \"month is not null in unreported mode\").to.eql(null);",
                  "});",
                  "pm.test(\"every ticket returned is genuinely unreported\", () => {",
                  "    // The whole contract of this mode. A reported ticket appearing here would be filed twice.",
                  "    const t = pm.response.json().tickets;",
                  "    const leaked = t.filter(x => x.reported !== false);",
                  "    pm.expect(leaked.map(x => x.ticket_number),",
                  "        \"an ALREADY REPORTED ticket came back in the unreported list -- it would be filed twice\").to.eql([]);",
                  "});",
                  "pm.test(\"rows still carry the full shape\", () => {",
                  "    // A second selection path is a second chance to serve a different shape. It must not.",
                  "    const rows = pm.response.json().tickets.flatMap(t => t.rows);",
                  "    if (!rows.length) { pm.expect(true).to.eql(true); return; }   // \"everything is filed\" is legal",
                  "    [\"pickup_date\",\"client_code\",\"client_name\",\"address\",\"city\",\"state\",\"zip\",\"county\",",
                  "     \"truck\",\"truck_decal\",\"truck_capacity_gallons\",\"gallons\",\"visit_id\",",
                  "     \"in_scope\",\"anomaly\",\"pickup_in_dade\"]",
                  "      .forEach(k => pm.expect(rows.every(r => k in r),",
                  "          \"unreported mode is missing \" + k + \" from its rows\").to.eql(true));",
                  "});",
                  "",
                  "pm.test(\"content holds across EVERY month, not just the last one\", () => {",
                  "    // 🛑 Why this is not redundant with the month= request. Selection is the ONLY branch",
                  "    // between the two modes; ordering, grouping, row mapping and accounting are shared",
                  "    // unbranched code. What differs is DATA REACH: month= exercises ~90 rows of one month,",
                  "    // this serves ~599 rows across 8 months and contains shapes the month test never sees",
                  "    // (null-state rows, extra anomaly rows, far more null decals, more accent carriers).",
                  "    // A data defect confined to any month but the last completed one passed the whole suite.",
                  "    const j = pm.response.json();",
                  "    const rows = j.tickets.flatMap(t => t.rows);",
                  "    if (!rows.length) { pm.expect(true).to.eql(true); return; }",
                  "",
                  "    const DECAL = { \"Moises\": \"C1184\", \"David\": \"C0976\" };",
                  "    const CAP   = { \"Moises\": 3840, \"David\": 1800, \"Cloggy\": 126, \"Goliath\": 4800 };",
                  "",
                  "    // 2026-09-18: gallons are no longer always null. White (Miami-Dade offload) rows must stay",
                  "    // null; yellow rows carry a trap capacity or null, never 0, and gallons_source names the arm.",
                  "    const rowsK = j.tickets.flatMap(t => t.rows.map(r => ({ ...r, _off: t.offload_in_dade })));",
                  "    pm.expect(rowsK.filter(r => r._off === true && r.gallons !== null).map(r => r.client_code),",
                  "        \"a white row carries gallons; the consumer would take it over the county invoice\").to.eql([]);",
                  "    pm.expect(rowsK.filter(r => (r.gallons === null) !== (r.gallons_source === null)).map(r => r.client_code),",
                  "        \"gallons and gallons_source disagree on null\").to.eql([]);",
                  "    pm.expect(rowsK.filter(r => r.gallons !== null && !(Number.isInteger(r.gallons) && r.gallons > 0)).map(r => r.client_code + '=' + r.gallons),",
                  "        \"a gallons value is not a positive integer\").to.eql([]);",
                  "    j.tickets.forEach(t => pm.expect(t.offload_in_dade === true ? t.dade_pickup_gallons === null : typeof t.dade_pickup_gallons === 'object' && t.dade_pickup_gallons !== null,",
                  "        'dade_pickup_gallons shape on ' + t.ticket_number).to.eql(true));",
                  "",
                  "    pm.expect(rows.filter(r => DECAL[r.truck] !== undefined && r.truck_decal !== DECAL[r.truck])",
                  "              .map(r => r.truck + \"=\" + JSON.stringify(r.truck_decal)),",
                  "        \"a truck reports the WRONG decal in unreported mode; this is the permit number that reaches the county\").to.eql([]);",
                  "    pm.expect(rows.filter(r => DECAL[r.truck] === undefined && r.truck_decal !== null)",
                  "              .map(r => JSON.stringify(r.truck) + \"=\" + JSON.stringify(r.truck_decal)),",
                  "        \"a row with no known decal carries one anyway; a guessed permit number\").to.eql([]);",
                  "",
                  "    pm.expect(rows.filter(r => CAP[r.truck] !== undefined && r.truck_capacity_gallons !== CAP[r.truck])",
                  "              .map(r => r.truck + \"=\" + r.truck_capacity_gallons),",
                  "        \"a truck reports the wrong capacity for itself, an internal consistency error\").to.eql([]);",
                  "",
                  "    // ⚠ state is a USPS two-letter code OR null. Null is REAL and is not a passthrough: it",
                  "    // arrives together with address, city, zip and county on the no-property case. Asserting",
                  "    // \"always two letters\" here would be a false failure, and asserting nothing would hide a",
                  "    // regression, so assert the true rule.",
                  "    pm.expect(rows.filter(r => r.state !== null && !/^[A-Z]{2}$/.test(String(r.state)))",
                  "              .map(r => r.client_code + \"=\" + JSON.stringify(r.state)),",
                  "        \"state is neither a two-letter USPS code nor null\").to.eql([]);",
                  "    const noAddr = rows.filter(r => r.state === null);",
                  "    pm.expect(noAddr.filter(r => !(r.address === null && r.city === null && r.zip === null && r.county === null))",
                  "              .map(r => r.client_code),",
                  "        \"state is null but the rest of the address is not; the no-property case must be all-or-nothing\").to.eql([]);",
                  "    if (noAddr.length) console.log(\"NOTE: \" + noAddr.length + \" row(s) carry no service address at all. \" +",
                  "        \"The null-decal rule already forces most of those tickets to be refused, but check any that survives it.\");",
                  "",
                  "    const nums = j.tickets.map(t => String(t.ticket_number));",
                  "    pm.expect(nums.length - new Set(nums).size,",
                  "        \"the same ticket appears twice; it would be filed twice\").to.eql(0);",
                  "    pm.expect(j.ticket_count, \"ticket_count disagrees with the tickets served\").to.eql(j.tickets.length);",
                  "    pm.expect(j.row_count, \"row_count disagrees with the rows served\").to.eql(rows.length);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Monthly - month + unreported together -> 400 (never a silent preference)",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "x-rpa-key",
                "value": "{{rpaBotKey}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly?month={{reportMonth}}&unreported=1",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly"
              ],
              "query": [
                {
                  "key": "month",
                  "value": "{{reportMonth}}"
                },
                {
                  "key": "unreported",
                  "value": "1"
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"400, not a silent preference for one of them\", () => {",
                  "    // ⚠ A parameter the caller supplied being quietly ignored is how someone ends up believing",
                  "    // they filtered when they did not. Refusing is the safe answer.",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error).to.eql(\"month_and_unreported_are_exclusive\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Monthly - month out of range -> 400",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Guards against a caller pulling a year we hold no data for and reading an empty month as",
                  "// \"nothing to file\". Refusing is the honest answer.",
                  "pm.test(\"400 month_out_of_range\", () => {",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error).to.eql(\"month_out_of_range\");",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly?month=2019-01",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly"
              ],
              "query": [
                {
                  "key": "month",
                  "value": "2019-01"
                }
              ]
            }
          }
        }
      ]
    },
    {
      "name": "6. Mark as reported (write) - dry-run safe, idempotent",
      "item": [
        {
          "name": "Filed - record a dry-run filing",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-dryrun\",\n  \"tickets\": [\n    \"{{lwtTicket}}\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"invoice_id\": \"SP00013840\",\n  \"confirmation_ref\": null,\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// ⚠ Run \"5. Monthly - filing set\" FIRST so {{lwtTicket}} is captured.",
                  "// 🛑 STABLE run_id on purpose: re-running this folder for ever creates exactly ONE row.",
                  "// dry_run:true means the filing is recorded but NEVER marks a ticket reported.",
                  "pm.test(\"200, and it is recorded (or already was)\", () => {",
                  "    pm.response.to.have.status(200);",
                  "    const j = pm.response.json();",
                  "    // Either answer is correct: first run records it, every later run replays it.",
                  "    pm.expect(j.recorded === true || j.already_recorded === true,",
                  "        \"neither recorded nor already_recorded -- the write did not happen\").to.eql(true);",
                  "    pm.expect(typeof j.filing_id, \"no filing_id returned\").to.eql(\"number\");",
                  "});",
                  "pm.test(\"a dry run does NOT mark the ticket reported\", function (done) {",
                  "    // 🛑 The safety property of the whole folder. A rehearsal must never make a real ticket",
                  "    // look filed to staff, or to the next unreported pull.",
                  "    // 🛑 THE `done` PARAMETER IS LOAD-BEARING. Without it postman-sandbox takes the synchronous",
                  "    // path: it calls this function, which only FIRES the request, and reports the test as",
                  "    // PASSED. The expects below would then run after the result was recorded and attach to",
                  "    // nothing. Under newman that failure is swallowed with no output and exit code 0, so the",
                  "    // one assertion protecting real tickets from a rehearsal would be permanently green.",
                  "    const t = pm.collectionVariables.get(\"lwtTicket\");",
                  "    pm.sendRequest({",
                  "        url: pm.collectionVariables.get(\"baseUrl\") + \"/rpa-derm-monthly?unreported=1\",",
                  "        method: \"GET\",",
                  "        header: { \"x-rpa-key\": pm.collectionVariables.get(\"rpaBotKey\") },",
                  "    }, function (err, res) {",
                  "        try {",
                  "            pm.expect(err, \"could not re-read the unreported list\").to.eql(null);",
                  "            const still = res.json().tickets.some(x => String(x.ticket_number) === String(t));",
                  "            pm.expect(still,",
                  "                \"a DRY RUN marked ticket \" + t + \" as reported -- it would be dropped from a real filing\").to.eql(true);",
                  "            done();",
                  "        } catch (e) { done(e); }",
                  "    });",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Filed - replay the same run_id -> idempotent, changes nothing",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-dryrun\",\n  \"tickets\": [\n    \"{{lwtTicket}}\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"invoice_id\": \"SP00013840\",\n  \"confirmation_ref\": null,\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"200 already_recorded, and recorded is false\", () => {",
                  "    // 🛑 Idempotency is enforced by a UNIQUE index, not a look-then-write, so two concurrent",
                  "    // replays cannot both insert. This is the normal answer to a retry, never an error.",
                  "    pm.response.to.have.status(200);",
                  "    const j = pm.response.json();",
                  "    pm.expect(j.already_recorded, \"a replay was not recognised -- a duplicate filing may exist\").to.eql(true);",
                  "    pm.expect(j.recorded, \"a replay reported itself as a fresh write\").to.eql(false);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Filed - an UNKNOWN ticket is recorded and echoed, never rejected",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-unknown\",\n  \"tickets\": [\n    \"ZZZ-NOT-A-TICKET\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"invoice_id\": \"SP00013840\",\n  \"confirmation_ref\": null,\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Jonathan: \"any difference against the invoice becomes a finding in either direction --",
                  "// including the reverse case we can't detect today.\" Refusing an unknown ticket would",
                  "// DESTROY that finding, so it is stored and reported back instead.",
                  "pm.test(\"200, recorded, and the unknown ticket is echoed back\", () => {",
                  "    pm.response.to.have.status(200);",
                  "    const j = pm.response.json();",
                  "    pm.expect(j.recorded === true || j.already_recorded === true,",
                  "        \"an unknown ticket was REJECTED -- the discrepancy Jonathan asked for is destroyed\").to.eql(true);",
                  "    if (j.recorded === true) {",
                  "        pm.expect(j.unknown_tickets,",
                  "            \"an unresolvable ticket was NOT reported back -- the discrepancy is lost\").to.include(\"ZZZ-NOT-A-TICKET\");",
                  "    } else {",
                  "        // 🛑 THE ECHO CHECK IS INERT ON EVERY RUN AFTER THE FIRST. run_id is stable, so the",
                  "        // row is recorded once and for ever and the replay path returns no unknown_tickets",
                  "        // to inspect. The half above still runs and is the one that matters: an unknown",
                  "        // ticket is ACCEPTED, never rejected. Bump the run_id suffix below once to re-arm",
                  "        // the echo check. Do not make this silent -- a skipped assertion reads as a passing",
                  "        // one in the run report.",
                  "        console.log(\"NOTE: unknown_tickets echo NOT re-verified, this run replayed an \" +",
                  "            \"existing run_id. Bump the run_id suffix in this body to test the echo again.\");",
                  "    }",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Filed - actor markers must agree -> 400",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-actor\",\n  \"tickets\": [\n    \"{{lwtTicket}}\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"invoice_id\": \"SP00013840\",\n  \"confirmation_ref\": null,\n  \"dry_run\": true,\n  \"filed_by_email\": \"someone@unclogme.com\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"400 actor_markers_disagree\", () => {",
                  "    // filed_by_email must be present exactly when run_id starts with \"manual-\". The DB carries",
                  "    // the same CHECK, so the two markers can never disagree about whether a person did this.",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error).to.eql(\"actor_markers_disagree\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Filed - period reversed -> 400",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-period\",\n  \"tickets\": [\n    \"{{lwtTicket}}\"\n  ],\n  \"period_start\": \"2026-07-25\",\n  \"period_end\": \"2026-06-28\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"invoice_id\": \"SP00013840\",\n  \"confirmation_ref\": null,\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"400 period_end_before_period_start\", () => {",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error).to.eql(\"period_end_before_period_start\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Filed - unknown field -> 400 (a typo is never ignored)",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-typo\",\n  \"tickets\": [\n    \"{{lwtTicket}}\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"invoice_id\": \"SP00013840\",\n  \"confirmation_ref\": null,\n  \"dry_run\": true,\n  \"confirmaton_ref\": \"typo on purpose\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"400 unknown_fields\", () => {",
                  "    // ⚠ A misspelled field name silently ignored is how a caller believes something was",
                  "    // recorded that was not. Same accept-and-reject discipline as rpa-derm-result.",
                  "    pm.response.to.have.status(400);",
                  "    const j = pm.response.json();",
                  "    pm.expect(j.error).to.eql(\"unknown_fields\");",
                  "    pm.expect(j.fields, \"the offending field is not named\").to.include(\"confirmaton_ref\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Filed - GET -> 405",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"405 method_not_allowed\", () => {",
                  "    pm.response.to.have.status(405);",
                  "    pm.expect(pm.response.json().error).to.eql(\"method_not_allowed\");",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Filed - body is an array -> 400 invalid_body",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// req.json() succeeds on an array, so this gets past invalid_json and is caught by the",
                  "// explicit Array.isArray guard. Worth its own test because the two failure modes look",
                  "// identical from outside and only one of them means \"your serialiser is wrong\".",
                  "pm.test(\"400 invalid_body\", () => {",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error,",
                  "        \"wrong error code: the request failed at a DIFFERENT gate than this test targets\").to.eql(\"invalid_body\");",
                  "});",
                  "pm.test(\"nothing was recorded\", () => {",
                  "    // Every validation gate sits BEFORE the insert, so a 400 must never leave a row.",
                  "    pm.expect(pm.response.json().recorded, \"a rejected request reported a write\").to.eql(undefined);",
                  "    pm.expect(pm.response.json().filing_id, \"a rejected request returned a filing_id\").to.eql(undefined);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "body": {
              "mode": "raw",
              "raw": "[\n  \"not\",\n  \"an object\"\n]",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            }
          }
        },
        {
          "name": "Filed - malformed run_id -> 400 invalid_run_id",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// run_id must match ^[A-Za-z0-9_.-]{1,100}$. It is the idempotency key, so a run_id that",
                  "// varies by whitespace would defeat replay detection and let a package be filed twice.",
                  "pm.test(\"400 invalid_run_id\", () => {",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error,",
                  "        \"wrong error code: the request failed at a DIFFERENT gate than this test targets\").to.eql(\"invalid_run_id\");",
                  "});",
                  "pm.test(\"the expected pattern is returned\", () => {",
                  "    pm.expect(pm.response.json().expected, \"no `expected` pattern to build against\").to.be.a(\"string\");",
                  "});",
                  "pm.test(\"nothing was recorded\", () => {",
                  "    // Every validation gate sits BEFORE the insert, so a 400 must never leave a row.",
                  "    pm.expect(pm.response.json().recorded, \"a rejected request reported a write\").to.eql(undefined);",
                  "    pm.expect(pm.response.json().filing_id, \"a rejected request returned a filing_id\").to.eql(undefined);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"has spaces and !\",\n  \"tickets\": [\n    \"{{lwtTicket}}\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            }
          }
        },
        {
          "name": "Filed - unparseable filed_at -> 400 invalid_filed_at",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// filed_at is when YOU filed, and it lands on a compliance record, so it is parsed rather",
                  "// than trusted. Everything before this gate is deliberately valid.",
                  "pm.test(\"400 invalid_filed_at\", () => {",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error,",
                  "        \"wrong error code: the request failed at a DIFFERENT gate than this test targets\").to.eql(\"invalid_filed_at\");",
                  "});",
                  "pm.test(\"nothing was recorded\", () => {",
                  "    // Every validation gate sits BEFORE the insert, so a 400 must never leave a row.",
                  "    pm.expect(pm.response.json().recorded, \"a rejected request reported a write\").to.eql(undefined);",
                  "    pm.expect(pm.response.json().filing_id, \"a rejected request returned a filing_id\").to.eql(undefined);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-errtest\",\n  \"tickets\": [\n    \"{{lwtTicket}}\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"last Tuesday\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            }
          }
        },
        {
          "name": "Filed - empty tickets -> 400 tickets_required",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// A filing with no tickets is the shape that would otherwise sit in the table for ever",
                  "// marking nothing as reported. Refused at the door.",
                  "pm.test(\"400 tickets_required\", () => {",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error,",
                  "        \"wrong error code: the request failed at a DIFFERENT gate than this test targets\").to.eql(\"tickets_required\");",
                  "});",
                  "pm.test(\"nothing was recorded\", () => {",
                  "    // Every validation gate sits BEFORE the insert, so a 400 must never leave a row.",
                  "    pm.expect(pm.response.json().recorded, \"a rejected request reported a write\").to.eql(undefined);",
                  "    pm.expect(pm.response.json().filing_id, \"a rejected request returned a filing_id\").to.eql(undefined);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-errtest\",\n  \"tickets\": [],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            }
          }
        },
        {
          "name": "Filed - over the ticket cap -> 400 too_many_tickets",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Built here rather than pasted, so the collection file does not carry a 2001-element array.",
                  "const n = 2001;",
                  "pm.collectionVariables.set(\"bulkTickets\",",
                  "    JSON.stringify(Array.from({ length: n }, (_, i) => \"T\" + i)));"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// The cap is 2000. Deliberately one over, so this test still fails if the cap is RAISED,",
                  "// rather than silently passing on a wider limit.",
                  "pm.test(\"400 too_many_tickets\", () => {",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error,",
                  "        \"wrong error code: the request failed at a DIFFERENT gate than this test targets\").to.eql(\"too_many_tickets\");",
                  "});",
                  "pm.test(\"the cap is reported back\", () => {",
                  "    pm.expect(pm.response.json().max, \"no `max` returned, so a caller cannot batch to fit\").to.eql(2000);",
                  "});",
                  "pm.test(\"nothing was recorded\", () => {",
                  "    // Every validation gate sits BEFORE the insert, so a 400 must never leave a row.",
                  "    pm.expect(pm.response.json().recorded, \"a rejected request reported a write\").to.eql(undefined);",
                  "    pm.expect(pm.response.json().filing_id, \"a rejected request returned a filing_id\").to.eql(undefined);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-errtest\",\n  \"tickets\": {{bulkTickets}},\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            }
          }
        },
        {
          "name": "Filed - illegal ticket characters -> 400 invalid_ticket_numbers",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// 🛑 THIS ONE IS A SECURITY BOUNDARY, not just hygiene. Ticket numbers are interpolated",
                  "// into a PostgREST .or() filter, so comma, parenthesis, quote and dot-notation must never",
                  "// reach it. The pattern ^[A-Za-z0-9_.-]{1,64}$ excludes all of them.",
                  "pm.test(\"400 invalid_ticket_numbers\", () => {",
                  "    pm.response.to.have.status(400);",
                  "    pm.expect(pm.response.json().error,",
                  "        \"wrong error code: the request failed at a DIFFERENT gate than this test targets\").to.eql(\"invalid_ticket_numbers\");",
                  "});",
                  "pm.test(\"the offending tickets are named\", () => {",
                  "    const bad = pm.response.json().tickets;",
                  "    pm.expect(Array.isArray(bad), \"no list of rejected tickets\").to.eql(true);",
                  "    pm.expect(bad, \"the valid ticket was rejected too\").to.not.include(pm.collectionVariables.get(\"lwtTicket\"));",
                  "});",
                  "pm.test(\"nothing was recorded\", () => {",
                  "    // Every validation gate sits BEFORE the insert, so a 400 must never leave a row.",
                  "    pm.expect(pm.response.json().recorded, \"a rejected request reported a write\").to.eql(undefined);",
                  "    pm.expect(pm.response.json().filing_id, \"a rejected request returned a filing_id\").to.eql(undefined);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-errtest\",\n  \"tickets\": [\n    \"{{lwtTicket}}\",\n    \"has space\",\n    \"quote\\\"inside\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            }
          }
        },
        {
          "name": "Filed - wrong key -> 401",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/rpa-derm-monthly-filed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "rpa-derm-monthly-filed"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"run_id\": \"bot-postman-badkey\",\n  \"tickets\": [\n    \"{{lwtTicket}}\"\n  ],\n  \"period_start\": \"2026-06-28\",\n  \"period_end\": \"2026-07-25\",\n  \"filed_at\": \"2026-08-26T19:40:00.000Z\",\n  \"invoice_id\": \"SP00013840\",\n  \"confirmation_ref\": null,\n  \"dry_run\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "auth": {
              "type": "apikey",
              "apikey": [
                {
                  "key": "key",
                  "value": "x-rpa-key",
                  "type": "string"
                },
                {
                  "key": "value",
                  "value": "{{wrongRpaKey}}",
                  "type": "string"
                },
                {
                  "key": "in",
                  "value": "header",
                  "type": "string"
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test(\"401 unauthorized\", () => {",
                  "    // 🛑 And it must be 401 BEFORE any write. A bad key that still recorded a filing would be",
                  "    // an unauthenticated write to compliance data.",
                  "    pm.response.to.have.status(401);",
                  "    pm.expect(pm.response.json().error).to.eql(\"unauthorized\");",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    }
  ]
}