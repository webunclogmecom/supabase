# Work log — @Building Apps · Sun 2026-08-16 → Mon 2026-08-17

*Written Mon 2026-08-17 05:59 ET. Continues [`WORK-LOG_2026-08-13_to_08-14.md`](WORK-LOG_2026-08-13_to_08-14.md).*

**Scope.** My work starts at **Sun 08-16 08:03** (`b6b8d7a`). Everything before that on 08-15 and
early 08-16 — the Admin Review "Send email to City" button, the pdf-service, the FP service-report
labels and pagination — is **@Supabase 2's**, and is not claimed here. We share repos, so a plain
`git log` would hand me their work.

---

## 1. Field Portal print report — visual design (Fred handed me the lane)

**Fred:** *"i don't like how it looks so work with BA Session, to come to agreement to make the report
look good."*

🛑 **This page is what a municipality receives.** Admin Review renders it to PDF and emails it as the
Job Completion Report, so every decision was made for a compliance document first.

@Supabase 2 handed over five measured defects. **I re-verified each rather than taking them on trust;
all five stood**, including the two they had inferred (DISPOSAL was a 19px section containing only the
word "DISPOSAL"; eyebrow labels were 1.33px tracking against a 2-3px spec).

**Shipped** (two prompts): Manrope across report + print stylesheet, photo grid 2 → 3 columns at
natural aspect, eyebrow labels 10px/700/2.5px, DISPOSAL renders **"Pending"**.

| check | before | after |
|---|---|---|
| off-brand fonts in the report | Plus Jakarta + `ui-serif` | **0 of 27** |
| letterboxed photo tiles | **6 of 14** | **0** |
| photos per row | 2 | 3 |
| DISPOSAL when empty | bare 19px heading | **"DISPOSAL Pending"** |

### 🛑 The decision they left to me, and why it went that way

They offered `object-fit: cover` or auto-height. **Auto-height, and the reason is compliance, not
taste.** `cover` fixes white bars by **cropping**, and here the photo IS the evidence: a crop can
silently remove the trap interior or fill line, and the municipality cannot tell anything was removed.
**An invisible loss of evidence is worse than an ugly margin.** The tile adapts to the photo.

### ✅ "Pending" is now APPROVED COPY

**Fred, 08-17: *"the 'Pending' wording is fine, keep it."*** No longer provisional. Settled
regulator-facing text — do not reword without asking him.

---

## 2. The city email told the regulator the wrong photo count

Found while opening the attachment at Fred's request. **The covering email said "(17 photos)"; the
attachment carried 14.**

```
before 5 + after 5 + extra 4 = 14   <- what the PDF carries
internal 3                          <- what it does NOT
                        total 17    <- what the email told the city
```

🛑 **The breakdown line was worse than the total** — the HTML body rendered **"Internal 3"**,
announcing to a municipality that photos exist which they are not being shown.

⚠ **There were TWO independent `Object.values(counts)` sums**, HTML and plain text. I fixed the HTML
one and **my own control caught that the plain-text body — the part actually delivered — still said
17**. Both now share one `CUSTOMER_PHASES` definition. Deployed (`48d13b2`), `verify_jwt` unchanged.

**Standing rule: count what is in the ATTACHMENT, never what we hold.**

---

## 3. Fred's two layout rules for the report, and my failed fix

**Fred, on the printed PDF:** *"i don't want cropped images or separated from their sections into
different pages if it can be avoided."* `ADDITIONAL PHOTOS (4)` printed 3 photos, a gap, then the 4th
**alone on the next page**, cut from its heading.

| rule | why it is a compliance property |
|---|---|
| never crop a photo | the photo IS the evidence |
| never split a section across pages when avoidable | an orphaned photo loses the heading saying what it is |

🛑 **My `break-inside: avoid` fix DID NOT WORK, and I reported it as done off the CSSOM.** Fred caught
it. Measured afterwards: the rule matched 3 sections, computed `avoid`, and the section was **515px in
a ~979px printable page** — and Chromium split it anyway, because **the ancestor's `avoid` does not
survive an inner CSS Grid**.

✅ **@Supabase 2 shipped my proposed print-flow fix** (grid → block/inline-block in `@media print`) and
**verified it page by page in the rendered PDF**: all three sections whole, page count stayed 3, 14
photos, none cropped. FP released. They also withdrew their earlier 3-page call under Fred's cohesion
rule.

---

## 4. Visit Calendar — five changes

| # | change | verified |
|---|---|---|
| 1 | **Skip confirmation dialog** | copy states future dates are unchanged |
| 2 | **Drawer close X**: 28×28 tap target → 48×48, **glyph 2rem**, `aria-label="Close"` added | live |
| 3 | **Footer stacks in a column on phones**, 12px gaps, 44px targets, divider above Delete | classes verified |
| 4 | **Hover card on EVERY chip** — Month 0→192, ANYTIME 0→15, grid 5/5, Day 2/2 | live |
| 5 | **Removed the duplicate native tooltip** the hover rollout created | Month 192→0 root titles |

### 🛑 The Skip dialog makes a factual promise, so I verified it before writing it

`skip_visit` → `fn_promote_next_sa_occurrence`, and **neither writes `visit_date`**. Then proved it
end to end on 112-YA (created a visit, skipped it through the real dialog, un-skipped, deleted it):

```
across the full round trip:  visit_date changes: 0     visit_status changes: 2
```

The same query caught both status changes, so **0 date changes is a real absence, not a blind
instrument**. ⚠ Skip is *not* side-effect-free — the next occurrence is pushed to Jobber. That is
plumbing, kept out of the dialog, but **do not let the copy drift into "nothing else happens"**.

---

## 5. Three of today's defects were MINE

Worth recording plainly, because they share one cause: **I changed a surface without first checking
what that surface already had.**

| # | what I did | how it surfaced |
|---|---|---|
| 1 | Added the hover card to chips that already carried a native `title` → **two tooltips at once**, the exact thing Fred rejected in July | Fred's screenshot |
| 2 | Reported the section-cohesion fix as done from the **CSSOM**, when the print still split | Fred re-checked the PDF |
| 3 | Read "make the X bigger" as the **hit area**, and only took the glyph to 24px | Fred: *"i didn't meant the clickable spot"* |

On #3 I kept the tap-target work anyway: at 28×28 with **no `aria-label`** it was a genuine defect,
just not the one he meant.

---

## 6. Broken instruments — five today, each caught only by a control

This was the day's real theme. **Every one of these produced a confident, wrong reading.**

| instrument | what it wrongly said | what caught it |
|---|---|---|
| `record_pk = '7765'` (text vs **jsonb**) | "the audit trail is empty" | `visits` has 223 audit rows in 24h |
| synthetic `hover` | "Month has no hover card" — but **0 on the grid block too**, where it works | running the control on the working surface |
| `app_source='jobber-anomaly-reconcile'` | "the reconciler never touched a Calendar visit" | the real label is `jobber-daily-**anomaly**-reconcile` |
| `getComputedStyle` on `document.body` | "Manrope never applied" | the report is a separate `.svcrep` subtree |
| **`\s` in a Postgres regex over this transport** | "the address has a non-space before the zip" | `Harding\s+Ave` **also** false on a verified plain space |

The last one is today's addition and closes @Supabase 2's open question: the codepoint between `FL`
and `33154` is **32, a plain ASCII space**. **The address data is clean** — their `FL33154` is a PDF
kerning artifact, not a missing character.

⚠ **A zero on BOTH sides of a partition proves nothing.** That single check would have caught three
of the five above before they became claims.

### Two environment traps worth carrying

- **Lovable deploy lag ran ~8 minutes**, and twice a change read as "not applied" before flipping.
  **Do not re-prompt on the first zeros.** One build ran ~40 minutes.
- **`resize_window` is unreliable**: it applied once going down to 390px, then refused to restore,
  leaving the window at 401px with a stuck Pixel-9 emulation that only a fresh tab cleared. It needed
  Fred to maximize the window.

---

## 7. Cross-session channel

⚠ **`send_message` failed six-plus times today, in both directions.** Fred had to relay by hand
twice, and I twice reported "no message received" when a message existed.

✅ **The fix, now a standing rule in root `CLAUDE.md` §5.2a-bis and in memory:** read the other
session's transcript with `mcp__ccd_session_mgmt__list_events`. It also beats the rendered card, which
truncates behind "Show more". **Never answer "no message received" without checking their transcript.**

---

## 8. State at end of day

**Everything asked for is shipped, published and verified. No open claim from this session; every
Lovable project is free.**

Open, and not mine:
- **`pgFactory` now applies a 60s INACTIVITY timeout by default** to every caller, including the 8
  scripts @Supabase 2 was wiring up. A merely-slow query looks like a hung socket. They need
  `timeoutMs: 0` for the heavy ones; I offered to make it opt-in.
- **Manrope in the PDF byte stream** is inferred, not measured. One `pdffonts` run settles it.

### Commit index (mine only)

**Building Apps:** `b6b8d7a` `c991d9e` `42a5c9e` `a987556` `caa38f6` `237215f` `0cf79cb` `8ca5be6`
`e361905` `2c20028` `ada8c8e` `7a7c7c6` `71609c3` `be654be` `d507e07`
**Supabase:** `48d13b2`
**Workspace root:** `d8854b2` `81e29e9` `0b6cf9e` `9dcca9c` `769a606` `bfc3bd0` `b1cd30a` `38eb967`
`b64f7df` `b2e43f4` `46ea362` `324f538` `f0bd704` `c251194` `bcf51d5` `823f959`

Detail lives in each app's `docs/08-changelog.md` and in `WORKING-NOW.md`.
